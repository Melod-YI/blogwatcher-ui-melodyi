---
phase: 19-blog-settings-enhancement
plan: 03
type: execute
wave: 3
depends_on: [19-01, 19-02]
files_modified: [internal/server/handlers.go]
autonomous: true
requirements: [SETT-05]
---

<objective>
扩展 handleUpdateBlogName handler 解析 URL 参数并更新数据库，实现 D-03a 空值允许和 SETT-05 立即更新数据库。

**Purpose:** 用户提交编辑表单后，后端验证 URL 参数并立即更新数据库中的 Blog URL 和 Feed URL 字段。

**Output:** handlers.go 包含 URL 参数解析、验证逻辑、数据库更新调用。
</objective>

<execution_context>
@C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-blog-settings-enhancement/19-CONTEXT.md
@docs/superpowers/specs/2026-05-09-blog-settings-enhancement-design.md

# 现有代码模式参考
@internal/server/handlers.go（handleUpdateBlogName 现有逻辑）
@internal/storage/database.go（UpdateBlog 方法）
</context>

<must_haves>
## Truths

- handleUpdateBlogName 解析 url 和 feed_url 参数
- 后端验证 URL 格式（HTTP/HTTPS，空值允许）
- 使用 database.UpdateBlog 方法更新所有字段
- 更新成功后返回 blog-display-row.gohtml 刷新卡片
- 错误时返回 400 Bad Request

## Success Criteria

1. 保存后立即更新数据库（SETT-05）
2. 后端验证 URL 格式（HTTP/HTTPS）
3. 空值允许（D-03a）
</must_haves>

<tasks>

<task type="auto">
<name>Task 01: 扩展 handleUpdateBlogName 解析 URL 参数并验证</name>
<files>internal/server/handlers.go</files>

<read_first>
- C:\workspace\blogwatcher-ui-melodyi\blogwatcher-ui\internal\server\handlers.go（handleUpdateBlogName 现有逻辑，line 658-728）
- C:\workspace\blogwatcher-ui-melodyi\blogwatcher-ui\internal\storage\database.go（UpdateBlog 方法，line 807-818）
- C:\workspace\blogwatcher-ui-melodyi\blogwatcher-ui\docs\superpowers\specs\2026-05-09-blog-settings-enhancement-design.md（验证规则）
</read_first>

<action>
扩展 handleUpdateBlogName handler，在 name 验证后添加 URL 参数解析和验证逻辑（per SETT-05 和 D-03a）。

**具体实现位置：**

在 handlers.go line 681（name 验证后）添加 URL 参数解析和验证：

```go
// 解析 URL 参数（per SETT-05）
url := strings.TrimSpace(r.FormValue("url"))
feedURL := strings.TrimSpace(r.FormValue("feed_url"))

// 验证 URL 格式（per SETT-04 和 D-03）
if err := validateURL(url); err != nil {
    http.Error(w, err.Error(), http.StatusBadRequest)
    return
}
if err := validateURL(feedURL); err != nil {
    http.Error(w, err.Error(), http.StatusBadRequest)
    return
}
```

**验证函数（添加在 handlers.go 文件末尾）：**

```go
// validateURL 验证 URL 格式（HTTP/HTTPS），空值允许（per D-03a）
func validateURL(url string) error {
    if url == "" {
        return nil // 空值允许（nullable 字段）
    }
    if !strings.HasPrefix(url, "http://") && !strings.HasPrefix(url, "https://") {
        return fmt.Errorf("URL 必须以 http:// 或 https:// 开头")
    }
    return nil
}
```

**关键验证要求（per D-03a 和 D-04）：**
- url 和 feed_url 参数解析
- 空值允许（url == "" 时返回 nil）
- 验证 http:// 或 https:// 前缀
- 错误时返回 400 Bad Request
</action>

<acceptance_criteria>
- handlers.go 包含 `url := strings.TrimSpace(r.FormValue("url"))`
- handlers.go 包含 `feedURL := strings.TrimSpace(r.FormValue("feed_url"))`
- handlers.go 包含 validateURL 函数定义
- validateURL 函数包含空值处理（`if url == "" { return nil }`）
- validateURL 函数验证 HTTP/HTTPS 前缀
- handlers.go 调用 validateURL(url) 和 validateURL(feedURL)
- URL 验证失败时返回 400 错误
</acceptance_criteria>

<verify>
<automated>grep -c "validateURL" internal/server/handlers.go</automated>
</verify>

<done>
handlers.go 包含 URL 参数解析、验证逻辑，错误时返回 400。
</done>
</task>

<task type="auto">
<name>Task 02: 扩展 handleUpdateBlogName 使用 UpdateBlog 更新所有字段</name>
<files>internal/server/handlers.go</files>

<read_first>
- C:\workspace\blogwatcher-ui-melodyi\blogwatcher-ui\internal\server\handlers.go（handleUpdateBlogName 现有逻辑）
- C:\workspace\blogwatcher-ui-melodyi\blogwatcher-ui\internal\storage\database.go（UpdateBlog 方法）
- C:\workspace\blogwatcher-ui-melodyi\blogwatcher-ui\internal\model/model.go（Blog struct）
</read_first>

<action>
替换 handleUpdateBlogName 中的 UpdateBlogName + UpdateBlogCategory 调用为 UpdateBlog 方法，一次性更新所有字段（per SETT-05）。

**具体实现位置：**

在 handlers.go line 682-701（现有的 UpdateBlogName + UpdateBlogCategory 调用）替换为：

```go
// 获取当前 blog 数据（保留其他字段不变）
blog, err := s.db.GetBlogByID(id)
if err != nil {
    log.Printf("Error fetching blog %d: %v", id, err)
    http.Error(w, "Database error", http.StatusInternalServerError)
    return
}
if blog == nil {
    http.Error(w, "Blog not found", http.StatusNotFound)
    return
}

// 更新字段（per SETT-05）
blog.Name = name
blog.URL = url
blog.FeedURL = feedURL

// 更新分类（per Phase 17）
categoryIDStr := r.FormValue("category_id")
var categoryID *int64
if categoryIDStr != "" {
    catID, err := strconv.ParseInt(categoryIDStr, 10, 64)
    if err != nil {
        log.Printf("Invalid category ID '%s': %v", categoryIDStr, err)
        // Non-blocking - ignore invalid category IDs
    } else {
        categoryID = &catID
    }
}
blog.CategoryID = categoryID

// 使用 UpdateBlog 一次性更新所有字段（per SETT-05）
if err := s.db.UpdateBlog(*blog); err != nil {
    log.Printf("Error updating blog %d: %v", id, err)
    http.Error(w, "Database error", http.StatusInternalServerError)
    return
}

log.Printf("Updated blog %d: name='%s', url='%s', feed_url='%s', category=%v", 
    id, name, url, feedURL, categoryID)
```

**关键更新要求（per SETT-05）：**
- 使用 database.UpdateBlog 方法一次性更新所有字段
- 更新 name, url, feed_url, category_id
- 保持其他字段不变（scrape_selector, last_scanned）
- 记录日志包含所有更新字段
</action>

<acceptance_criteria>
- handlers.go 包含 `blog.Name = name`
- handlers.go 包含 `blog.URL = url`
- handlers.go 包含 `blog.FeedURL = feedURL`
- handlers.go 包含 `blog.CategoryID = categoryID`
- handlers.go 调用 `s.db.UpdateBlog(*blog)`
- handlers.go 包含日志记录所有更新字段
- 不再调用 UpdateBlogName 和 UpdateBlogCategory（使用 UpdateBlog 替代）
</acceptance_criteria>

<verify>
<automated>grep -c "UpdateBlog.*blog" internal/server/handlers.go</automated>
</verify>

<done>
handlers.go 使用 UpdateBlog 一次性更新所有字段，日志记录完整。
</done>
</task>

</tasks>

<verification>
**手动验证：**
1. 启动 UI 服务器
2. 访问设置页面，点击 Edit 进入编辑模式
3. 修改 Blog URL 或 Feed URL：
   - 输入有效 URL（https://example.com）
   - 点击 Save
   - 检查卡片是否刷新显示新 URL
   - 检查数据库是否更新（可通过 CLI `blogwatcher blog list` 查看）
4. 输入无效 URL（ftp://example.com）：
   - 点击 Save
   - 检查是否返回 400 错误
5. 输入空值：
   - 清空 Feed URL 输入框
   - 点击 Save
   - 检查数据库 FeedURL 字段是否为空（允许空值 per D-03a）
</verification>

<success_criteria>
- [ ] URL 参数正确解析
- [ ] URL 验证生效（HTTP/HTTPS 前缀）
- [ ] 空值允许（不报错）
- [ ] 无效 URL 返回 400 错误
- [ ] UpdateBlog 方法成功更新所有字段
- [ ] 日志记录所有更新字段
- [ ] 卡片刷新显示新 URL
</success_criteria>

<output>
After completion, create `.planning/phases/19-blog-settings-enhancement/19-03-SUMMARY.md`
</output>