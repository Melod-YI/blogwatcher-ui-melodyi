---
phase: 18
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - internal/storage/database.go
  - internal/server/handlers.go
  - internal/server/routes.go
  - assets/templates/partials/category-group.gohtml
  - assets/templates/partials/sidebar.gohtml
autonomous: true
requirements:
  - CATG-08
  - CATG-09

must_haves:
  truths:
    - "Subscriptions 区域按分类分组显示 blog"
    - "未分类 blog 显示在底部（无分类标签，有分隔线）"
    - "空分类不显示"
    - "点击分类名称可展开/折叠该分类下的 blog"
  artifacts:
    - path: "internal/storage/database.go"
      provides: "GroupedSidebarData 结构 + ListBlogsGroupedByCategory 方法"
      contains: "func (db *Database) ListBlogsGroupedByCategory"
      min_lines: 30
    - path: "assets/templates/partials/category-group.gohtml"
      provides: "分类分组渲染模板"
      contains: "category-group"
    - path: "assets/templates/partials/sidebar.gohtml"
      provides: "改造使用分类分组"
      contains: "category-group.gohtml"
  key_links:
    - from: "internal/server/handlers.go"
      to: "internal/storage/database.go"
      via: "ListBlogsGroupedByCategory"
      pattern: "ListBlogsGroupedByCategory"
    - from: "assets/templates/partials/sidebar.gohtml"
      to: "assets/templates/partials/category-group.gohtml"
      via: "template 引用"
      pattern: "template \"category-group"
---

<objective>
实现 Subscriptions 区域按分类分层展示（CATG-08, CATG-09）。

Purpose: 用户可以在 sidebar 看到按分类分组的 blog 列表，分类在前、未分类在后。
Output: 分类分组渲染模板，handler 组装分组数据，sidebar 使用新模板。

Per D-01: 分类在前，未分类在后（类似原型方案C）
Per D-02: 未分类 blog 显示在底部，有分隔线 + "未分类" 标签
Per D-03: 空分类不显示（模板渲染时过滤 blog_count > 0）
Per D-04: 分类标题可展开/折叠（样式在本 plan 定义，JS 在 Plan 03 实现）
Per D-05: 默认全部展开（初始渲染无 collapsed class）
</objective>

<execution_context>
@C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-category-display-cli/18-CONTEXT.md
@.planning/phases/18-category-display-cli/18-RESEARCH.md
@.planning/phases/18-category-display-cli/18-PATTERNS.md
@.planning/phases/18-category-display-cli/18-UI-SPEC.md

<interfaces>
<!-- Key types and contracts executor needs -->

From internal/storage/database.go (existing patterns):
```go
// CategoryWithBlogCount 结构 (Phase 17 已添加)
type CategoryWithBlogCount struct {
    Category  model.Category
    BlogCount int
}

// BlogWithCount 结构 (Phase 9 已添加)
type BlogWithCount struct {
    model.Blog
    ArticleCount int
    CategoryID   *int64  // Phase 16 已添加
}

// ListBlogsWithCounts 方法 (line 356-414)
func (db *Database) ListBlogsWithCounts() ([]BlogWithCount, error)

// ListCategoriesWithBlogCount 方法 (Phase 17 已添加)
func (db *Database) ListCategoriesWithBlogCount() ([]CategoryWithBlogCount, error)
```

From assets/templates/partials/blog-list.gohtml (HTMX pattern):
```html
<a href="/articles?blog={{.ID}}"
   hx-get="/articles?blog={{.ID}}"
   hx-target="#main-content"
   hx-push-url="true"
   hx-on:click="document.querySelectorAll('.sidebar-nav .nav-link, .blog-item').forEach(el => el.classList.remove('active')); this.classList.add('active'); document.getElementById('sidebar-toggle').checked = false;"
   class="blog-item{{if eq $.CurrentBlogID .ID}} active{{end}}">
    {{.Name}}
</a>
```

From assets/templates/partials/sidebar.gohtml (line 62-71):
```html
<div class="subscriptions-section">
    <div class="nav-section-title">Subscriptions</div>
    <div id="blog-list"
         hx-get="/blogs"
         hx-trigger="blogListUpdated from:body"
         hx-swap="innerHTML">
        {{template "blog-list.gohtml" .}}
    </div>
</div>
```

From internal/server/handlers.go (handleBlogList pattern):
```go
func (s *Server) handleBlogList(w http.ResponseWriter, r *http.Request) {
    blogs, err := s.db.ListBlogs()
    // ...
    data := map[string]interface{}{
        "Blogs": blogs,
    }
    s.renderTemplate(w, "blog-list.gohtml", data)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 GroupedSidebarData 结构和 ListBlogsGroupedByCategory 方法</name>
  <files>internal/storage/database.go</files>
  <read_first>
    - internal/storage/database.go (查看 CategoryWithBlogCount 结构)
    - internal/storage/database.go (查看 BlogWithCount 结构)
    - internal/storage/database.go (查看 ListBlogsWithCounts 方法)
    - internal/storage/database.go (查看 ListCategoriesWithBlogCount 方法)
    - internal/model/blog.go (查看 Blog 结构体的 CategoryID 字段)
  </read_first>
  <action>
    Per D-01: 分类在前，未分类在后
    Per D-03: 空分类不显示（过滤 blog_count > 0）

    修改 internal/storage/database.go:

    1. 在 CategoryWithBlogCount 定义附近添加新结构体:
    ```go
    // CategoryWithBlogs extends CategoryWithBlogCount with blog list for sidebar grouping.
    type CategoryWithBlogs struct {
        Category  model.Category
        BlogCount int
        Blogs     []BlogWithCount
    }

    // GroupedSidebarData holds data for category-grouped sidebar rendering.
    type GroupedSidebarData struct {
        Categories    []CategoryWithBlogs  // blog_count > 0, sorted alphabetically
        Uncategorized []BlogWithCount      // category_id = NULL, sorted alphabetically
        CurrentBlogID int64                // 当前选中的 blog ID（用于 active 状态）
    }
    ```

    2. 在 ListCategoriesWithBlogCount 方法后添加新方法:
    ```go
    // ListBlogsGroupedByCategory returns blogs grouped by category for sidebar rendering.
    // Categories with blog_count > 0 first, uncategorized blogs at the end.
    // Both groups are sorted alphabetically by name.
    func (db *Database) ListBlogsGroupedByCategory(currentBlogID int64) (GroupedSidebarData, error) {
        // 1. 获取分类 + blog count
        categories, err := db.ListCategoriesWithBlogCount()
        if err != nil {
            return GroupedSidebarData{}, err
        }
        
        // 2. 获取所有 blogs + category_id
        blogs, err := db.ListBlogsWithCounts()
        if err != nil {
            return GroupedSidebarData{}, err
        }
        
        // 3. 分组逻辑
        var grouped []CategoryWithBlogs
        var uncategorized []BlogWithCount
        
        // 按分类分组
        for _, cat := range categories {
            if cat.BlogCount > 0 {  // Per D-03: 空分类不显示
                var catBlogs []BlogWithCount
                for _, blog := range blogs {
                    if blog.CategoryID != nil && *blog.CategoryID == cat.Category.ID {
                        catBlogs = append(catBlogs, blog)
                    }
                }
                grouped = append(grouped, CategoryWithBlogs{
                    Category:  cat.Category,
                    BlogCount: cat.BlogCount,
                    Blogs:     catBlogs,
                })
            }
        }
        
        // 未分类 blogs (category_id = NULL)
        for _, blog := range blogs {
            if blog.CategoryID == nil {
                uncategorized = append(uncategorized, blog)
            }
        }
        
        return GroupedSidebarData{
            Categories:    grouped,
            Uncategorized: uncategorized,
            CurrentBlogID: currentBlogID,
        }, nil
    }
    ```
  </action>
  <verify>
    <automated>grep -n "type GroupedSidebarData struct" internal/storage/database.go | grep -c . == 1</automated>
    <automated>grep -n "func (db \*Database) ListBlogsGroupedByCategory" internal/storage/database.go | grep -c . == 1</automated>
    <automated>grep -n "cat.BlogCount > 0" internal/storage/database.go | grep -c . == 1</automated>
  </verify>
  <acceptance_criteria>
    - GroupedSidebarData struct 存在，包含 Categories、Uncategorized、CurrentBlogID 字段
    - CategoryWithBlogs struct 存在，包含 Category、BlogCount、Blogs 字段
    - ListBlogsGroupedByCategory 方法存在
    - 方法中过滤空分类: `cat.BlogCount > 0`
  </acceptance_criteria>
  <done>分组数据结构和查询方法实现完成。</done>
</task>

<task type="auto">
  <name>Task 2: 新增 handleBlogListGrouped handler 和 route</name>
  <files>internal/server/handlers.go, internal/server/routes.go</files>
  <read_first>
    - internal/server/handlers.go (查看 handleBlogList 方法，line 153-183)
    - internal/server/routes.go (查看 route 注册模式，line 17)
    - internal/storage/database.go (查看 ListBlogsGroupedByCategory 方法)
  </read_first>
  <action>
    新增 handleBlogListGrouped handler 用于返回分类分组数据。

    修改 internal/server/handlers.go:

    1. 在 handleBlogList 方法附近添加新 handler:
    ```go
    // handleBlogListGrouped serves the blog list grouped by category
    // Returns partial fragment for HTMX requests
    func (s *Server) handleBlogListGrouped(w http.ResponseWriter, r *http.Request) {
        // 获取当前选中的 blog ID（从 query 参数）
        currentBlogIDStr := r.URL.Query().Get("blog")
        var currentBlogID int64
        if currentBlogIDStr != "" {
            id, err := strconv.ParseInt(currentBlogIDStr, 10, 64)
            if err == nil {
                currentBlogID = id
            }
        }
        
        // 获取分组数据
        grouped, err := s.db.ListBlogsGroupedByCategory(currentBlogID)
        if err != nil {
            log.Printf("Error fetching grouped blogs: %v", err)
            http.Error(w, "Database error", http.StatusInternalServerError)
            return
        }
        
        data := map[string]interface{}{
            "Grouped": grouped,
        }
        
        // 返回 partial fragment
        s.renderTemplate(w, "category-group.gohtml", data)
    }
    ```

    修改 internal/server/routes.go:

    2. 在 route 注册中添加（约 line 17 后）:
    ```go
    s.mux.HandleFunc("GET /blogs/grouped", s.handleBlogListGrouped)
    ```
  </action>
  <verify>
    <automated>grep -n "func (s \*Server) handleBlogListGrouped" internal/server/handlers.go | grep -c . == 1</automated>
    <automated>grep -n "GET /blogs/grouped" internal/server/routes.go | grep -c . == 1</automated>
    <automated>grep -n "ListBlogsGroupedByCategory" internal/server/handlers.go | grep -c . == 1</automated>
  </verify>
  <acceptance_criteria>
    - handleBlogListGrouped handler 存在
    - routes.go 包含 `GET /blogs/grouped` route
    - handler 调用 ListBlogsGroupedByCategory
    - handler 渲染 category-group.gohtml template
  </acceptance_criteria>
  <done>Handler 和 route 注册完成，可响应 /blogs/grouped 请求。</done>
</task>

<task type="auto">
  <name>Task 3: 创建 category-group.gohtml 分类分组模板</name>
  <files>assets/templates/partials/category-group.gohtml</files>
  <read_first>
    - assets/templates/partials/blog-list.gohtml (查看 HTMX 导航模式)
    - assets/templates/partials/sidebar.gohtml (查看现有结构)
    - .planning/phases/18-category-display-cli/18-UI-SPEC.md (查看模板结构定义)
  </read_first>
  <action>
    Per D-01: 分类在前，未分类在后
    Per D-02: 未分类显示在底部，分隔线 + "未分类" 标签
    Per D-03: 空分类不显示（已在 ListBlogsGroupedByCategory 中过滤）
    Per D-04: 分类标题可展开/折叠（hx-on:click 调用 toggleCategory）
    Per D-05: 默认全部展开（无 collapsed class）

    创建 assets/templates/partials/category-group.gohtml:

    ```html
    {{define "category-group.gohtml"}}
    {{/* ABOUTME: Renders blogs grouped by category for sidebar.
         ABOUTME: Categories first (blog_count > 0), uncategorized at the end with separator. */}}
    
    {{/* 分类分组（已在 database 层过滤 blog_count > 0） */}}
    {{range .Grouped.Categories}}
    <div class="category-group" data-category="{{.Category.Name}}">
        <div class="category-header"
             role="button"
             aria-expanded="true"
             aria-controls="category-blogs-{{.Category.ID}}"
             tabindex="0"
             hx-on:click="toggleCategory(this)">
            <svg class="chevron-icon" aria-hidden="true" width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polyline points="6 9 12 15 18 9"></polyline>
            </svg>
            <span class="category-name">{{.Category.Name}}</span>
            <span class="category-count">{{.BlogCount}}</span>
        </div>
        <div id="category-blogs-{{.Category.ID}}" class="category-blogs">
            {{range .Blogs}}
            <a href="/articles?blog={{.ID}}"
               hx-get="/articles?blog={{.ID}}"
               hx-target="#main-content"
               hx-push-url="true"
               hx-on:click="document.querySelectorAll('.sidebar-nav .nav-link, .blog-item').forEach(el => el.classList.remove('active')); this.classList.add('active'); document.getElementById('sidebar-toggle').checked = false;"
               class="blog-item{{if eq $.Grouped.CurrentBlogID .ID}} active{{end}}">
                {{.Name}}
            </a>
            {{end}}
        </div>
    </div>
    {{end}}
    
    {{/* 未分类 blog（分隔线 + "未分类" 标签） */}}
    {{if .Grouped.Uncategorized}}
    <div class="uncategorized-section">
        <div class="uncategorized-header">未分类</div>
        {{range .Grouped.Uncategorized}}
        <a href="/articles?blog={{.ID}}"
           hx-get="/articles?blog={{.ID}}"
           hx-target="#main-content"
           hx-push-url="true"
           hx-on:click="document.querySelectorAll('.sidebar-nav .nav-link, .blog-item').forEach(el => el.classList.remove('active')); this.classList.add('active'); document.getElementById('sidebar-toggle').checked = false;"
           class="blog-item{{if eq $.Grouped.CurrentBlogID .ID}} active{{end}}">
            {{.Name}}
        </a>
        {{end}}
    </div>
    {{end}}
    
    {{/* 空状态 */}}
    {{if not .Grouped.Categories}}{{if not .Grouped.Uncategorized}}
    <p class="empty-state">No blogs tracked yet. Use the blogwatcher CLI to add blogs.</p>
    {{end}}{{end}}
    {{end}}
    ```

    关键点:
    - 分类标题有 aria-expanded="true" (D-05 默认展开)
    - hx-on:click="toggleCategory(this)" (D-04 折叠交互，JS 在 Plan 03)
    - 未分类有 uncategorized-section 分隔线和 "未分类" 标签 (D-02)
  </action>
  <verify>
    <automated>test -f assets/templates/partials/category-group.gohtml && echo "exists"</automated>
    <automated>grep -n "category-group.gohtml" assets/templates/partials/category-group.gohtml | grep -c . == 1</automated>
    <automated>grep -n "uncategorized-section" assets/templates/partials/category-group.gohtml | grep -c . == 1</automated>
    <automated>grep -n "toggleCategory" assets/templates/partials/category-group.gohtml | grep -c . == 1</automated>
  </verify>
  <acceptance_criteria>
    - category-group.gohtml 文件存在
    - 模板包含 {{define "category-group.gohtml"}}
    - 分类分组容器有 class="category-group" 和 data-category 属性
    - 分类标题有 aria-expanded="true" 和 hx-on:click="toggleCategory(this)"
    - 未分类区域有 class="uncategorized-section" 和 "未分类" 标签
    - blog-item 保持现有 HTMX 导航模式
  </acceptance_criteria>
  <done>分类分组模板创建完成，渲染逻辑符合设计规范。</done>
</task>

<task type="auto">
  <name>Task 4: 改造 sidebar.gohtml 使用分类分组渲染</name>
  <files>assets/templates/partials/sidebar.gohtml</files>
  <read_first>
    - assets/templates/partials/sidebar.gohtml (查看现有结构，line 62-71)
    - assets/templates/partials/category-group.gohtml (新创建的模板)
  </read_first>
  <action>
    改造 sidebar.gohtml 的 Subscriptions section 使用分类分组渲染。

    修改 assets/templates/partials/sidebar.gohtml:

    将 line 62-71 的 Subscriptions section 改为:
    ```html
    {{/* Subscriptions / Blog list - Category grouped */}}
    <div class="subscriptions-section">
        <div class="nav-section-title">Subscriptions</div>
        <div id="blog-list"
             hx-get="/blogs/grouped?blog={{.CurrentBlogID}}"
             hx-trigger="blogListUpdated from:body"
             hx-swap="innerHTML">
            {{template "category-group.gohtml" .}}
        </div>
    </div>
    ```

    关键改动:
    - hx-get 从 "/blogs" 改为 "/blogs/grouped?blog={{.CurrentBlogID}}"
    - template 从 "blog-list.gohtml" 改为 "category-group.gohtml"
    - 传递 CurrentBlogID 用于 active 状态判断

    注意: handleIndex 需要确保传递 CurrentBlogID 到模板数据（已有，line 78）。
  </action>
  <verify>
    <automated>grep -n "hx-get=\"/blogs/grouped" assets/templates/partials/sidebar.gohtml | grep -c . == 1</automated>
    <automated>grep -n "category-group.gohtml" assets/templates/partials/sidebar.gohtml | grep -c . == 1</automated>
  </verify>
  <acceptance_criteria>
    - sidebar.gohtml 中 hx-get="/blogs/grouped" 替换原有 "/blogs"
    - sidebar.gohtml 使用 {{template "category-group.gohtml" .}}
    - hx-get 包含 blog={{.CurrentBlogID}} query 参数
  </acceptance_criteria>
  <done>Sidebar 改造完成，使用分类分组渲染。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server | HTMX request to /blogs/grouped, XSS 防护由 Go template 自动处理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering | Category name in template | mitigate | Go template 自动 HTML escape |
| T-18-04 | Information Disclosure | Blog list data | accept | 无敏感数据，公开信息 |
</threat_model>

<verification>
## Per-Task Verification

| Task | Command |
|------|---------|
| Task 1 | `grep -n "GroupedSidebarData" internal/storage/database.go` |
| Task 2 | `grep -n "handleBlogListGrouped" internal/server/handlers.go` |
| Task 3 | `grep -n "category-group.gohtml" assets/templates/partials/category-group.gohtml` |
| Task 4 | `grep -n "/blogs/grouped" assets/templates/partials/sidebar.gohtml` |

## End-to-End

```bash
# 编译测试
go build -o blogwatcher ./cmd/blogwatcher

# 启动服务器
./blogwatcher serve &
sleep 2

# 测试分组渲染
curl -s http://localhost:8080/blogs/grouped | grep -q "category-group"

# 停止服务器
kill %1
```
</verification>

<success_criteria>
1. GroupedSidebarData 和 CategoryWithBlogs 结构存在
2. ListBlogsGroupedByCategory 方法返回分组数据，过滤 blog_count > 0
3. handleBlogListGrouped handler 注册 GET /blogs/grouped
4. category-group.gohtml 模板渲染分类分组和未分类区域
5. sidebar.gohtml 使用 /blogs/grouped 和 category-group.gohtml
6. grep 验证所有代码改动存在
</success_criteria>

<output>
After completion, create `.planning/phases/18-category-display-cli/18-02-SUMMARY.md`
</output>