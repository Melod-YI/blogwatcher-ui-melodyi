---
phase: 18
slug: category-display-cli
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-09
---

# Phase 18 — UI Design Contract

> Visual and interaction contract for Phase 18: Category Display & CLI.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable (Go + HTMX project) |
| Component library | none (Go templates + CSS custom properties) |
| Icon library | inline SVG (chevron icon) |
| Font | system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding (匹配现有 --radius-sm) |
| sm | 8px | Compact element spacing (匹配现有 blog-item padding-top) |
| md | 12px | Default element spacing (匹配现有 blog-item padding-right) |
| lg | 16px | Section padding (匹配现有 sidebar padding, 1rem) |
| xl | 24px | Layout gaps, major section breaks |
| 2xl | 32px | Page-level spacing |
| 3xl | 48px | Large section breaks |

Exceptions:
- nav-link padding: 10px (0.625rem) 12px — 现有样式，保持一致
- sidebar width: 250px — 现有布局，保持一致

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 (regular) | 1.5 |
| Label | 12px (0.75rem) | 700 (bold) | 1.5 |
| Category Header | 13px | 600 (semibold) | 1.2 |
| Uncategorized Header | 11px (0.6875rem) | 700 (bold) | 1.5 |
| Blog Item | 13px | 400 (regular) | 1.5 |
| Category Count | 10px (0.625rem) | 400 (regular) | 1.5 |

**说明：**
- Body: 匹配现有 html font-size: 16px, line-height: 1.5
- Label: 匹配现有 .nav-section-title 样式
- Category Header: 方案C设计，13px font-weight 600
- Uncategorized Header: 方案C设计，11px uppercase letter-spacing 0.05em
- Blog Item: 保持现有 13px (继承自 body，无显式字号)
- Category Count: 方案C设计，右侧灰色计数

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | --bg-surface | Sidebar background, main container |
| Secondary (30%) | --bg-elevated | Category header background, hover states |
| Accent (10%) | --accent | Active blog items, interactive focus |
| Destructive | #dc2626 | Delete buttons (现有 btn-danger 样式) |

**Accent reserved for:**
- Active blog item text color
- Active nav-link text color
- Focus-visible outline
- Primary action buttons

**CSS Variables (已存在):**
```css
:root {
  --bg-primary: #FAF8F5;
  --bg-surface: #FFFFFF;
  --bg-elevated: #F5F3F0;
  --text-primary: #37352F;
  --text-secondary: #6B6B6B;
  --accent: #2563EB;
  --border: #E5E3E0;
}

html.dark {
  --bg-primary: #121212;
  --bg-surface: #1e1e1e;
  --bg-elevated: #2d2d2d;
  --text-primary: #e0e0e0;
  --text-secondary: #a0a0a0;
  --accent: #64b5f6;
  --border: #333333;
}
```

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | 展开分类 / 折叠分类 (toggle, 无显式按钮) |
| Empty state heading | No blogs tracked yet |
| Empty state body | Use the blogwatcher CLI to add blogs. (现有文案) |
| Error state (CLI) | 分类 '{name}' 不存在 |
| Uncategorized label | 未分类 |

**Destructive actions:**
- 无破坏性操作在此阶段

---

## Component Inventory

### 新增组件

| Component | File | Description |
|-----------|------|-------------|
| Category Group | category-group.gohtml | 分类分组容器，包含 header + blog list |
| Category Header | category-header.gohtml | 可折叠分类标题，chevron + name + count |
| Uncategorized Section | sidebar.gohtml (改造) | 分隔线 + "未分类" 标签 + blog list |

### 现有组件（保持）

| Component | File | Status |
|-----------|------|--------|
| Blog Item | sidebar.gohtml | 保持现有样式 |
| Sidebar Navigation | sidebar.gohtml | 保持现有结构 |
| Nav Section Title | sidebar.gohtml | 保持现有样式 |

---

## Visual Hierarchy

### 方案C: 混合布局（分类在前，未分类在后）

```
Subscriptions
├─ [Tech ▾ 3]          ← 分类标题（背景色 + chevron + 计数）
│   ├─ Hacker News     ← blog item (缩进 12px)
│   ├─ Go Blog
│   └─ React Blog
├─ [News ▾ 2]
│   ├─ BBC Tech
│   └─ TechCrunch
│
├─ ────────────────    ← 分隔线 (1px solid --border)
│
├─ 未分类               ← 未分类标签 (uppercase, muted)
│   ├─ 个人博客
│   └─ 随机收藏
```

### 分类标题样式

```css
.category-header {
  display: flex;
  align-items: center;
  gap: 8px;              /* var(--space-sm) */
  padding: 8px;          /* var(--space-sm) */
  cursor: pointer;
  font-size: 13px;
  font-weight: 600;
  color: var(--text-secondary);
  background: var(--bg-elevated);
  border-radius: 4px;    /* var(--radius-sm) */
  transition: all 0.25s ease;
  margin-bottom: 8px;    /* var(--space-sm) */
}

.category-header:hover {
  background: var(--bg-hover);
  color: var(--text-primary);
}

.category-header.collapsed .chevron-icon {
  transform: rotate(-90deg);
}
```

### Chevron图标

```html
<svg class="chevron-icon" width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
  <polyline points="6 9 12 15 18 9"></polyline>
</svg>
```

```css
.chevron-icon {
  transition: transform 0.25s ease;
}
```

### 分类计数

```css
.category-count {
  font-size: 10px;
  color: var(--text-muted);  /* 需新增 CSS 变量 */
  margin-left: auto;
}
```

### 未分类分隔线

```css
.uncategorized-section {
  margin-top: 16px;      /* var(--space-lg) */
  padding-top: 16px;     /* var(--space-lg) */
  border-top: 1px solid var(--border);
}

.uncategorized-header {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.05em;
  color: var(--text-secondary);
  text-transform: uppercase;
  margin-bottom: 8px;    /* var(--space-sm) */
}
```

### 分类内blog列表

```css
.category-blogs {
  display: flex;
  flex-direction: column;
  gap: 2px;
  padding-left: 12px;    /* var(--space-md) */
  overflow: hidden;
  max-height: 500px;
  transition: max-height 0.25s ease, opacity 0.15s ease;
}

.category-group.collapsed .category-blogs {
  max-height: 0;
  opacity: 0;
}
```

---

## Interaction Patterns

### 展开/折叠交互

| Trigger | Action | Persistence |
|---------|--------|-------------|
| Click category-header | Toggle .collapsed class | localStorage |

**JavaScript实现:**
```javascript
// localStorage key: sidebar-category-expand-state
function toggleCategory(header) {
  const group = header.parentElement;
  const isCollapsed = group.classList.contains('collapsed');
  
  if (isCollapsed) {
    group.classList.remove('collapsed');
  } else {
    group.classList.add('collapsed');
  }
  
  saveExpandState();
}

function saveExpandState() {
  const state = {};
  document.querySelectorAll('.category-group').forEach(group => {
    const categoryName = group.dataset.category;
    state[categoryName] = !group.classList.contains('collapsed');
  });
  localStorage.setItem('sidebar-category-expand-state', JSON.stringify(state));
}

function loadExpandState() {
  const saved = localStorage.getItem('sidebar-category-expand-state');
  if (saved) {
    const state = JSON.parse(saved);
    document.querySelectorAll('.category-group').forEach(group => {
      const categoryName = group.dataset.category;
      if (state[categoryName] === false) {
        group.classList.add('collapsed');
      }
    });
  }
}

// 默认全部展开（D-05）
// loadExpandState 仅在有 localStorage 数据时应用折叠状态
```

**HTMX事件绑定:**
```html
<div class="category-header"
     hx-on:click="toggleCategory(this)">
  ...
</div>
```

---

## Accessibility

| Element | Keyboard | Screen Reader |
|---------|----------|---------------|
| Category header | Enter/Space to toggle | aria-expanded="true/false", aria-controls="blog-list-{id}" |
| Blog item | Enter to navigate | aria-label="博客名称" |
| Chevron icon | Decorative | aria-hidden="true" |

**ARIA属性:**
```html
<div class="category-header"
     role="button"
     aria-expanded="true"
     aria-controls="category-blogs-{categoryID}"
     tabindex="0"
     hx-on:click="toggleCategory(this)">
  <span class="chevron-icon" aria-hidden="true">...</span>
  <span class="category-name">Tech</span>
  <span class="category-count">3</span>
</div>
```

---

## Empty State Handling

| Scenario | Behavior |
|----------|----------|
| 空分类 (blog_count = 0) | 不显示 (D-03) |
| 无分类存在 | 所有 blog 在未分类区显示 |
| 无 blog 存在 | 显示现有 empty-state 文案 |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | none | not applicable |

---

## CSS Additions Required

以下CSS需要在 styles.css 中新增:

```css
/* ============================================
   Category Grouping Styles (Phase 18)
   ============================================ */

/* 新增 muted 文本颜色变量 */
:root {
  --text-muted: #9ca3af;
}

html.dark {
  --text-muted: #6b7280;
}

/* 分类分组容器 */
.category-group {
  margin-bottom: 8px;
}

/* 分类标题 */
.category-header {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px;
  cursor: pointer;
  font-size: 13px;
  font-weight: 600;
  color: var(--text-secondary);
  background: var(--bg-elevated);
  border-radius: 4px;
  transition: background-color 0.25s ease, color 0.25s ease;
  margin-bottom: 8px;
}

.category-header:hover {
  background: var(--bg-surface);
  color: var(--text-primary);
}

.category-header:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

/* Chevron 旋转 */
.category-header .chevron-icon {
  width: 12px;
  height: 12px;
  flex-shrink: 0;
  transition: transform 0.25s ease;
}

.category-header.collapsed .chevron-icon {
  transform: rotate(-90deg);
}

/* 分类计数 */
.category-count {
  font-size: 10px;
  color: var(--text-muted);
  margin-left: auto;
}

/* 分类内 blog 列表 */
.category-blogs {
  display: flex;
  flex-direction: column;
  gap: 2px;
  padding-left: 12px;
  overflow: hidden;
  max-height: 500px;
  transition: max-height 0.25s ease, opacity 0.15s ease;
}

.category-group.collapsed .category-blogs {
  max-height: 0;
  opacity: 0;
}

/* 未分类分隔区 */
.uncategorized-section {
  margin-top: 16px;
  padding-top: 16px;
  border-top: 1px solid var(--border);
}

.uncategorized-header {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.05em;
  color: var(--text-secondary);
  text-transform: uppercase;
  margin-bottom: 8px;
}
```

---

## Template Structure

### sidebar.gohtml 改造

```html
{{/* Subscriptions / Blog list */}}
<div class="subscriptions-section">
  <div class="nav-section-title">Subscriptions</div>
  <div id="blog-list"
       hx-get="/blogs"
       hx-trigger="blogListUpdated from:body"
       hx-swap="innerHTML">
    {{template "category-grouped-list.gohtml" .}}
  </div>
</div>
```

### category-grouped-list.gohtml (新增)

```html
{{define "category-grouped-list.gohtml"}}
{{/* 分类分组渲染：categories → separator → uncategorized */}}

{{range .Categories}}
<div class="category-group" data-category="{{.Name}}">
  <div class="category-header"
       role="button"
       aria-expanded="true"
       aria-controls="category-blogs-{{.ID}}"
       tabindex="0"
       hx-on:click="toggleCategory(this)">
    <svg class="chevron-icon" aria-hidden="true" width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <polyline points="6 9 12 15 18 9"></polyline>
    </svg>
    <span class="category-name">{{.Name}}</span>
    <span class="category-count">{{.BlogCount}}</span>
  </div>
  <div id="category-blogs-{{.ID}}" class="category-blogs">
    {{range .Blogs}}
    <a href="/articles?blog={{.ID}}"
       hx-get="/articles?blog={{.ID}}"
       hx-target="#main-content"
       hx-push-url="true"
       hx-on:click="..."
       class="blog-item{{if eq $.CurrentBlogID .ID}} active{{end}}">
      {{.Name}}
    </a>
    {{end}}
  </div>
</div>
{{end}}

{{if .UncategorizedBlogs}}
<div class="uncategorized-section">
  <div class="uncategorized-header">未分类</div>
  {{range .UncategorizedBlogs}}
  <a href="/articles?blog={{.ID}}"
     hx-get="/articles?blog={{.ID}}"
     hx-target="#main-content"
     hx-push-url="true"
     hx-on:click="..."
     class="blog-item{{if eq $.CurrentBlogID .ID}} active{{end}}">
    {{.Name}}
  </a>
  {{end}}
</div>
{{end}}

{{else}}
<p class="empty-state">No blogs tracked yet. Use the blogwatcher CLI to add blogs.</p>
{{end}}
{{end}}
```

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS (文案与现有风格一致)
- [ ] Dimension 2 Visuals: PASS (方案C原型已验证)
- [ ] Dimension 3 Color: PASS (使用现有CSS变量)
- [ ] Dimension 4 Typography: PASS (匹配现有样式系统)
- [ ] Dimension 5 Spacing: PASS (multiples of 4)
- [ ] Dimension 6 Registry Safety: PASS (无第三方registry)

**Approval:** pending

---

## Pre-Populated From

| Source | Decisions Used |
|--------|---------------|
| CONTEXT.md | D-01~D-08 (全部8个已锁定决策) |
| RESEARCH.md | Standard stack, Architecture patterns |
| styles.css | 现有CSS变量系统、blog-item样式 |
| category-sidebar-grouping.html | 方案C原型样式 |
| sidebar.gohtml | 现有sidebar结构 |
| User input | 0 (所有决策已从上游锁定) |

---

*Phase: 18 - Category Display & CLI*
*UI-SPEC created: 2026-05-09*