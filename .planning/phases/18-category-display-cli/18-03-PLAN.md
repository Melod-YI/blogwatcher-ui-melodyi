---
phase: 18
plan: 03
type: execute
wave: 3
depends_on:
  - 18-02
files_modified:
  - assets/static/styles.css
  - assets/js/sidebar.js
  - assets/templates/base.gohtml
autonomous: true
requirements:
  - CATG-08

must_haves:
  truths:
    - "点击分类名称可展开/折叠该分类下的 blog"
    - "折叠状态持久化到 localStorage"
    - "刷新页面后折叠状态保持"
    - "chevron 图标旋转动画流畅"
  artifacts:
    - path: "assets/static/styles.css"
      provides: "分类分组样式 + chevron 动画 + 分隔线"
      contains: ".category-header"
      min_lines: 50
    - path: "assets/js/sidebar.js"
      provides: "localStorage 折叠状态持久化"
      contains: "toggleCategory"
    - path: "assets/templates/base.gohtml"
      provides: "引入 sidebar.js"
      contains: "sidebar.js"
  key_links:
    - from: "assets/js/sidebar.js"
      to: "localStorage"
      via: "sidebar-category-expand-state key"
    - from: "assets/templates/base.gohtml"
      to: "assets/js/sidebar.js"
      via: "script src"
---

<objective>
实现分类标题展开/折叠交互和 localStorage 持久化（D-04, D-05）。

Purpose: 用户可以折叠分类以节省空间，状态持久化避免每次重新展开。
Output: CSS 样式、sidebar.js 持久化逻辑、base.gohtml 引入 JS。

Per D-04: 分类标题可展开/折叠，状态持久化到 localStorage
Per D-05: 默认全部展开（初始渲染无 collapsed class）

localStorage key: sidebar-category-expand-state
</objective>

<execution_context>
@C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-category-display-cli/18-CONTEXT.md
@.planning/phases/18-category-display-cli/18-RESEARCH.md
@.planning/phases/18-category-display-cli/18-PATTERNS.md
@.planning/phases/18-category-display-cli/18-UI-SPEC.md

<interfaces>
<!-- Key types and contracts executor needs -->

From assets/templates/base.gohtml (localStorage pattern):
```javascript
// 主题持久化模式 (line 5-22)
var stored = localStorage.getItem('theme') || 'system';
localStorage.setItem('theme', value);

// HTMX swap 后重新应用
document.body.addEventListener('htmx:afterSwap', function(evt) {
    // handle swap
});
```

From assets/static/styles.css (existing patterns):
```css
/* CSS 变量系统 */
:root {
  --bg-primary: #FAF8F5;
  --bg-surface: #FFFFFF;
  --bg-elevated: #F5F3F0;
  --text-primary: #37352F;
  --text-secondary: #6B6B6B;
  --border: #E5E3E0;
  --accent: #2563EB;
  --transition-speed: 150ms;
}

html.dark {
  --bg-primary: #121212;
  --bg-surface: #1e1e1e;
  --bg-elevated: #2d2d2d;
  --text-primary: #e0e0e0;
  --text-secondary: #a0a0a0;
  --border: #333333;
  --accent: #64b5f6;
}

/* blog-item 样式 (line 212-232) */
.blog-item {
  display: block;
  padding: 0.5rem 0.75rem;
  border-radius: 6px;
  color: var(--text-primary);
  text-decoration: none;
  transition: background-color var(--transition-speed) ease;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增分类分组 CSS 样式</name>
  <files>assets/static/styles.css</files>
  <read_first>
    - assets/static/styles.css (查看 CSS 变量定义，line 1-61)
    - assets/static/styles.css (查看 blog-item 样式，line 212-232)
    - assets/static/styles.css (查看 nav-section-title 样式，line 173-180)
    - .planning/phases/18-category-display-cli/18-UI-SPEC.md (查看 CSS 定义)
  </read_first>
  <action>
    Per D-02: 未分类分隔线样式
    Per D-04: 分类标题样式 + chevron 旋转动画
    Per D-05: 默认展开（无 collapsed class 时 chevron 不旋转）

    在 assets/static/styles.css 的 blog-item 样式块后添加:

    ```css
    /* ============================================
       Category Group Styles (Sidebar)
       ============================================ */

    /* 分类分组容器 */
    .category-group {
      margin-bottom: 0.5rem;
    }

    /* 分类标题 */
    .category-header {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.5rem;
      cursor: pointer;
      font-size: 0.875rem;
      font-weight: 600;
      color: var(--text-secondary);
      background-color: var(--bg-elevated);
      border-radius: 4px;
      transition: background-color 0.25s ease, color 0.25s ease;
      margin-bottom: 0.25rem;
    }

    .category-header:hover {
      background-color: var(--bg-surface);
      color: var(--text-primary);
    }

    .category-header:focus-visible {
      outline: 2px solid var(--accent);
      outline-offset: 2px;
    }

    /* Chevron 图标 */
    .category-header .chevron-icon {
      width: 12px;
      height: 12px;
      flex-shrink: 0;
      transition: transform 0.25s ease;
    }

    .category-header.collapsed .chevron-icon {
      transform: rotate(-90deg);
    }

    /* 分类计数 */
    .category-count {
      font-size: 0.75rem;
      color: var(--text-secondary);
      margin-left: auto;
    }

    /* 分类内 blog 列表 */
    .category-blogs {
      display: flex;
      flex-direction: column;
      gap: 2px;
      padding-left: 0.75rem;
      overflow: hidden;
      max-height: 500px;
      transition: max-height 0.25s ease, opacity 0.15s ease;
    }

    .category-group.collapsed .category-blogs {
      max-height: 0;
      opacity: 0;
    }

    /* 未分类分隔区 */
    .uncategorized-section {
      margin-top: 1rem;
      padding-top: 1rem;
      border-top: 1px solid var(--border);
    }

    .uncategorized-header {
      font-size: 0.75rem;
      font-weight: 600;
      letter-spacing: 0.05em;
      color: var(--text-secondary);
      text-transform: uppercase;
      margin-bottom: 0.25rem;
      padding: 0 0.75rem;
    }
    ```

    注意:
    - 所有颜色使用 CSS 变量，自动适配 light/dark 主题
    - transition 时间 0.25s (250ms) 与 UI-SPEC 一致
    - collapsed class 控制 chevron 旋转和 blog-list 隐藏
  </action>
  <verify>
    <automated>grep -n ".category-header" assets/static/styles.css | grep -c . >= 1</automated>
    <automated>grep -n ".category-group.collapsed" assets/static/styles.css | grep -c . == 1</automated>
    <automated>grep -n ".uncategorized-section" assets/static/styles.css | grep -c . == 1</automated>
    <automated>grep -n "chevron-icon" assets/static/styles.css | grep -c . >= 1</automated>
  </verify>
  <acceptance_criteria>
    - .category-header 样式存在，包含 hover、focus-visible 状态
    - .category-header.collapsed .chevron-icon 有 rotate(-90deg)
    - .category-group.collapsed .category-blogs 有 max-height: 0
    - .uncategorized-section 有 border-top 分隔线
    - .uncategorized-header 有 uppercase 和 letter-spacing
    - 所有颜色使用 CSS 变量 (var(--...))
  </acceptance_criteria>
  <done>CSS 样式添加完成，分类分组视觉效果实现。</done>
</task>

<task type="auto">
  <name>Task 2: 创建 sidebar.js localStorage 持久化逻辑</name>
  <files>assets/js/sidebar.js</files>
  <read_first>
    - assets/templates/base.gohtml (查看 localStorage 主题持久化模式)
    - assets/templates/partials/category-group.gohtml (查看 toggleCategory 调用)
    - .planning/phases/18-category-display-cli/18-UI-SPEC.md (查看 JS 实现规范)
  </read_first>
  <action>
    Per D-04: 折叠状态持久化到 localStorage
    Per D-05: 默认全部展开（loadExpandState 仅在有 localStorage 数据时应用折叠）

    创建 assets/js/sidebar.js:

    ```javascript
    // ABOUTME: Sidebar category expand/collapse persistence
    // ABOUTME: Uses localStorage to persist collapse state across sessions

    (function() {
      var STORAGE_KEY = 'sidebar-category-expand-state';

      // Toggle category expansion (called from template hx-on:click)
      function toggleCategory(header) {
        var group = header.parentElement;
        var isCollapsed = group.classList.contains('collapsed');

        if (isCollapsed) {
          group.classList.remove('collapsed');
          header.setAttribute('aria-expanded', 'true');
        } else {
          group.classList.add('collapsed');
          header.setAttribute('aria-expanded', 'false');
        }

        // Persist to localStorage
        saveExpandState();
      }

      // Save expand state to localStorage
      function saveExpandState() {
        var state = {};
        document.querySelectorAll('.category-group').forEach(function(group) {
          var categoryName = group.dataset.category;
          state[categoryName] = !group.classList.contains('collapsed');
        });
        localStorage.setItem(STORAGE_KEY, JSON.stringify(state));
      }

      // Load expand state from localStorage
      // Per D-05: 默认全部展开，仅在有 saved data 时应用折叠
      function loadExpandState() {
        var saved = localStorage.getItem(STORAGE_KEY);
        if (saved) {
          try {
            var state = JSON.parse(saved);
            document.querySelectorAll('.category-group').forEach(function(group) {
              var categoryName = group.dataset.category;
              if (state[categoryName] === false) {
                group.classList.add('collapsed');
                var header = group.querySelector('.category-header');
                if (header) {
                  header.setAttribute('aria-expanded', 'false');
                }
              }
            });
          } catch (e) {
            // Ignore parse errors
          }
        }
      }

      // Initialize on page load
      document.addEventListener('DOMContentLoaded', loadExpandState);

      // Re-apply after HTMX swaps (sidebar refreshes)
      document.body.addEventListener('htmx:afterSwap', function(evt) {
        if (evt.detail && evt.detail.target && evt.detail.target.id === 'blog-list') {
          loadExpandState();
        }
      });

      // Expose toggleCategory globally for hx-on:click handlers
      window.toggleCategory = toggleCategory;
    })();
    ```

    关键点:
    - STORAGE_KEY: 'sidebar-category-expand-state'
    - toggleCategory 更新 aria-expanded 属性
    - loadExpandState 仅在有 localStorage 数据时应用折叠（D-05）
    - htmx:afterSwap 监听 #blog-list swap，重新应用状态
    - window.toggleCategory 全局暴露供模板调用
  </action>
  <verify>
    <automated>test -f assets/js/sidebar.js && echo "exists"</automated>
    <automated>grep -n "toggleCategory" assets/js/sidebar.js | grep -c . == 3</automated>
    <automated>grep -n "sidebar-category-expand-state" assets/js/sidebar.js | grep -c . == 1</automated>
    <automated>grep -n "htmx:afterSwap" assets/js/sidebar.js | grep -c . == 1</automated>
    <automated>grep -n "window.toggleCategory" assets/js/sidebar.js | grep -c . == 1</automated>
  </verify>
  <acceptance_criteria>
    - sidebar.js 文件存在
    - toggleCategory 函数存在，切换 collapsed class 和 aria-expanded
    - STORAGE_KEY 为 'sidebar-category-expand-state'
    - saveExpandState 将状态存入 localStorage
    - loadExpandState 从 localStorage 恢复状态，仅应用折叠（D-05 默认展开）
    - htmx:afterSwap 监听 #blog-list 重新应用状态
    - window.toggleCategory 全局暴露
  </acceptance_criteria>
  <done>sidebar.js 创建完成，折叠状态持久化逻辑实现。</done>
</task>

<task type="auto">
  <name>Task 3: base.gohtml 引入 sidebar.js</name>
  <files>assets/templates/base.gohtml</files>
  <read_first>
    - assets/templates/base.gohtml (查看 script 引入模式)
    - assets/js/sidebar.js (确认文件路径)
  </read_first>
  <action>
    在 base.gohtml 中引入 sidebar.js。

    修改 assets/templates/base.gohtml:

    在 {{define "scripts"}} block 或 HTML 底部 script 区域添加:
    ```html
    <script src="/static/sidebar.js"></script>
    ```

    注意: static 文件由 routes.go 的 `/static/` route 服务 (line 12)。
  </action>
  <verify>
    <automated>grep -n "sidebar.js" assets/templates/base.gohtml | grep -c . == 1</automated>
  </verify>
  <acceptance_criteria>
    - base.gohtml 包含 `<script src="/static/sidebar.js"></script>`
  </acceptance_criteria>
  <done>sidebar.js 引入完成，页面加载时自动执行持久化逻辑。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → localStorage | 客户端状态存储，无敏感数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering | localStorage manipulation | accept | 无敏感数据，仅 UI 状态 |
</threat_model>

<verification>
## Per-Task Verification

| Task | Command |
|------|---------|
| Task 1 | `grep -n ".category-header" assets/static/styles.css` |
| Task 2 | `grep -n "toggleCategory" assets/js/sidebar.js` |
| Task 3 | `grep -n "sidebar.js" assets/templates/base.gohtml` |

## End-to-End (Manual Verification)

```bash
# 启动服务器
go run ./cmd/blogwatcher serve

# 手动验证步骤:
# 1. 打开 http://localhost:8080
# 2. 点击分类标题，观察:
#    - chevron 旋转 -90deg
#    - blog 列表隐藏
#    - aria-expanded 变为 "false"
# 3. 刷新页面
# 4. 验证折叠状态保持
```
</verification>

<success_criteria>
1. CSS .category-header 样式存在，包含 hover/focus-visible/collapsed 状态
2. CSS .uncategorized-section 分隔线样式存在
3. sidebar.js 实现 toggleCategory、saveExpandState、loadExpandState
4. localStorage key 为 'sidebar-category-expand-state'
5. htmx:afterSwap 监听重新应用状态
6. base.gohtml 引入 sidebar.js
7. grep 验证所有代码改动存在
</success_criteria>

<output>
After completion, create `.planning/phases/18-category-display-cli/18-03-SUMMARY.md`
</output>