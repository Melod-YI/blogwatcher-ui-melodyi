---
phase: 18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/storage/database.go
  - internal/cli/commands/article.go
autonomous: true
requirements:
  - CATG-10

must_haves:
  truths:
    - "CLI article list --category <name> 返回该分类下的所有文章"
    - "CLI article list --category tech --unread 组合过滤生效"
    - "CLI --category 不存在的分类时报错退出"
  artifacts:
    - path: "internal/storage/database.go"
      provides: "GetCategoryByName 方法 + ListFilterOptions.CategoryName 字段"
      contains: "func (db *Database) GetCategoryByName"
      min_lines: 20
    - path: "internal/cli/commands/article.go"
      provides: "--category flag + 验证逻辑"
      contains: "cmd.Flags().String(\"category\""
  key_links:
    - from: "internal/cli/commands/article.go"
      to: "internal/storage/database.go"
      via: "GetCategoryByName 调用"
      pattern: "GetCategoryByName"
---

<objective>
实现 CLI `article list --category` 分类过滤功能（CATG-10）。

Purpose: 用户可以通过 CLI 按分类名称筛选文章，与现有筛选参数组合使用。
Output: --category flag 可用，分类验证生效，筛选结果正确。

Per D-06: 按分类名称筛选（如 `--category tech`）
Per D-07: 统一组合过滤（加入 ListFilterOptions.CategoryName）
Per D-08: 验证分类存在性（查询分类表，不存在则报错退出）
</objective>

<execution_context>
@C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-category-display-cli/18-CONTEXT.md
@.planning/phases/18-category-display-cli/18-RESEARCH.md
@.planning/phases/18-category-display-cli/18-PATTERNS.md

<interfaces>
<!-- Key types and contracts executor needs -->

From internal/storage/database.go (existing patterns):
```go
// GetBlogByName 模式 (lines 659-663)
func (db *Database) GetBlogByName(name string) (*model.Blog, error) {
    row := db.conn.QueryRow(`SELECT id, name, url, feed_url, scrape_selector, last_scanned, category_id FROM blogs WHERE name = ?`, name)
    return scanBlog(row)
}

// ListFilterOptions 结构 (lines 1132-1140)
type ListFilterOptions struct {
    BlogName  string     // 博客名称筛选
    IsRead    *bool      // 已读状态筛选
    HasNote   *bool      // 备注状态筛选
    AfterDate *time.Time // 日期筛选
    Limit     int        // 结果数量限制
}

// ListArticlesWithFilters 方法 (lines 1144-1211)
// 添加筛选条件的模式:
if opts.BlogName != "" {
    conditions = append(conditions, "a.blog_id = (SELECT id FROM blogs WHERE name = ?)")
    args = append(args, opts.BlogName)
}
```

From internal/cli/commands/article.go (existing patterns):
```go
// Flag 定义 (line 70)
cmd.Flags().String("blog", "", "博客名称筛选")

// Flag 解析 (line 143)
blogName, _ := cmd.Flags().GetString("blog")

// 验证模式 (lines 188-200)
if blogName != "" && len(articles) == 0 {
    blog, err := db.GetBlogByName(blogName)
    if err != nil {
        fmt.Fprintf(os.Stderr, "查询博客失败: %v\n", err)
        os.Exit(1)
    }
    if blog == nil {
        fmt.Fprintf(os.Stderr, "博客 '%s' 不存在\n", blogName)
        os.Exit(1)
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 ListFilterOptions 结构添加 CategoryName 字段</name>
  <files>internal/storage/database.go</files>
  <read_first>
    - internal/storage/database.go (查看 ListFilterOptions 结构体定义，line 1132-1140)
    - internal/storage/database.go (查看 ListArticlesWithFilters 方法，line 1144-1211)
  </read_first>
  <action>
    Per D-07: 在 ListFilterOptions struct 中添加 CategoryName string 字段。

    修改 internal/storage/database.go:

    1. 在 ListFilterOptions 结构体中添加 CategoryName 字段（在 BlogName 字段后）:
    ```go
    type ListFilterOptions struct {
        BlogName     string     // 博客名称筛选（空表示所有博客）
        CategoryName string     // 分类名称筛选（空表示所有分类）  // 新增
        IsRead       *bool      // 已读状态筛选（nil 表示所有状态）
        HasNote      *bool      // 备注状态筛选（nil 表示所有状态）
        AfterDate    *time.Time // 日期筛选（nil 表示无限制）
        Limit        int        // 结果数量限制（0 表示无限制）
    }
    ```

    2. 在 ListArticlesWithFilters 方法中添加分类筛选条件（在 BlogName 筛选条件后）:
    ```go
    // 分类名称筛选（通过子查询获取 category_id）
    if opts.CategoryName != "" {
        conditions = append(conditions, "b.category_id = (SELECT id FROM categories WHERE name = ?)")
        args = append(args, opts.CategoryName)
    }
    ```

    注意：查询语句中 b.category_id 来自 blogs 表的 category_id 字段（Phase 16 已添加）。
  </action>
  <verify>
    <automated>grep -n "CategoryName" internal/storage/database.go | grep -c "string" == 1</automated>
    <automated>grep -n "b.category_id = (SELECT id FROM categories WHERE name" internal/storage/database.go | grep -c . == 1</automated>
  </verify>
  <acceptance_criteria>
    - ListFilterOptions struct 包含 CategoryName string 字段
    - ListArticlesWithFilters 方法包含分类筛选条件: "b.category_id = (SELECT id FROM categories WHERE name = ?)"
  </acceptance_criteria>
  <done>ListFilterOptions.CategoryName 字段存在，ListArticlesWithFilters 支持分类筛选。</done>
</task>

<task type="auto">
  <name>Task 2: 新增 GetCategoryByName 数据库方法</name>
  <files>internal/storage/database.go</files>
  <read_first>
    - internal/storage/database.go (查看 GetBlogByName 方法实现，line 659-663)
    - internal/storage/database.go (查看 scanCategory 函数，确认是否存在)
    - internal/model/category.go (查看 Category 结构体定义)
  </read_first>
  <action>
    Per D-08: 新增 GetCategoryByName 方法用于 CLI 验证分类存在性。

    修改 internal/storage/database.go:

    1. 在 GetBlogByName 方法附近（约 line 660 后）添加 GetCategoryByName 方法:
    ```go
    // GetCategoryByName returns a category by its name, or nil if not found.
    func (db *Database) GetCategoryByName(name string) (*model.Category, error) {
        row := db.conn.QueryRow(`SELECT id, name, created_at FROM categories WHERE name = ?`, name)
        return scanCategory(row)
    }
    ```

    2. 检查 scanCategory 函数是否已存在（Phase 17 应已添加）。如果不存在，添加:
    ```go
    // scanCategory scans a category row from the database.
    func scanCategory(scanner interface{ Scan(dest ...any) error }) (*model.Category, error) {
        var (
            id        int64
            name      string
            createdAt sql.NullString
        )
        if err := scanner.Scan(&id, &name, &createdAt); err != nil {
            if errors.Is(err, sql.ErrNoRows) {
                return nil, nil
            }
            return nil, err
        }
        
        category := &model.Category{
            ID:   id,
            Name: name,
        }
        if createdAt.Valid {
            if parsed, err := parseTime(createdAt.String); err == nil {
                category.CreatedAt = parsed
            }
        }
        return category, nil
    }
    ```

    错误消息格式（D-08）: 分类 '{name}' 不存在（与现有 "博客 '{name}' 不存在" 风格一致）
  </action>
  <verify>
    <automated>grep -n "func (db \*Database) GetCategoryByName" internal/storage/database.go | grep -c . == 1</automated>
    <automated>grep -n "scanCategory" internal/storage/database.go | grep -c . >= 1</automated>
  </verify>
  <acceptance_criteria>
    - GetCategoryByName 方法存在: `func (db *Database) GetCategoryByName(name string) (*model.Category, error)`
    - scanCategory 函数存在（用于扫描 Category 行）
    - GetCategoryByName 查询语句: `SELECT id, name, created_at FROM categories WHERE name = ?`
  </acceptance_criteria>
  <done>GetCategoryByName 方法实现完成，可返回分类或 nil。</done>
</task>

<task type="auto">
  <name>Task 3: CLI article.go 新增 --category flag 和验证逻辑</name>
  <files>internal/cli/commands/article.go</files>
  <read_first>
    - internal/cli/commands/article.go (查看 NewListCmd 函数，line 41-81)
    - internal/cli/commands/article.go (查看 runList 函数，line 122-214)
    - internal/cli/commands/article.go (查看 --blog 验证模式，line 188-200)
  </read_first>
  <action>
    Per D-06: 新增 --category flag（按分类名称筛选）
    Per D-08: 验证分类存在性（复用 --blog 验证模式）

    修改 internal/cli/commands/article.go:

    1. 在 NewListCmd 的 Long 描述中添加 --category 说明（line 45-65）:
    在"筛选参数："列表中添加:
    ```
      --category <name> 按分类名称筛选
    ```
    在"示例："中添加:
    ```
      blogwatcher article list --category tech --unread
    ```

    2. 在 cmd.Flags() 添加（约 line 70 后）:
    ```go
    cmd.Flags().String("category", "", "分类名称筛选")
    ```

    3. 在 runList 函数中解析 category flag（约 line 143 后）:
    ```go
    categoryName, _ := cmd.Flags().GetString("category")
    ```

    4. 在 opts 构建中添加 CategoryName（约 line 151 后）:
    ```go
    opts := storage.ListFilterOptions{
        BlogName:     blogName,
        CategoryName: categoryName,  // 新增
    }
    ```

    5. 在 --blog 验证逻辑后（约 line 200 后）添加分类验证:
    ```go
    // 检查分类名称是否存在（如果指定了）
    if categoryName != "" {
        category, err := db.GetCategoryByName(categoryName)
        if err != nil {
            fmt.Fprintf(os.Stderr, "查询分类失败: %v\n", err)
            os.Exit(1)
        }
        if category == nil {
            fmt.Fprintf(os.Stderr, "分类 '%s' 不存在\n", categoryName)
            os.Exit(1)
        }
    }
    ```

    错误消息格式:
    - 查询失败: "查询分类失败: %v"
    - 不存在: "分类 '{name}' 不存在"
  </action>
  <verify>
    <automated>grep -n "cmd.Flags().String(\"category\"" internal/cli/commands/article.go | grep -c . == 1</automated>
    <automated>grep -n "categoryName, _ := cmd.Flags().GetString(\"category\")" internal/cli/commands/article.go | grep -c . == 1</automated>
    <automated>grep -n "CategoryName:" internal/cli/commands/article.go | grep -c . == 1</automated>
    <automated>grep -n "GetCategoryByName" internal/cli/commands/article.go | grep -c . == 1</automated>
    <automated>grep -n "分类 '%s' 不存在" internal/cli/commands/article.go | grep -c . == 1</automated>
  </verify>
  <acceptance_criteria>
    - --category flag 定义存在: `cmd.Flags().String("category", "", "分类名称筛选")`
    - categoryName 解析存在: `categoryName, _ := cmd.Flags().GetString("category")`
    - opts 包含 CategoryName: `CategoryName: categoryName`
    - 分类验证调用 GetCategoryByName
    - 错误消息: "分类 '{name}' 不存在"
  </acceptance_criteria>
  <done>CLI --category flag 可用，验证逻辑实现完成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Database | 用户输入的分类名称通过 SQL 参数化查询，无 SQL 注入风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | --category input | mitigate | SQL 参数化查询（`WHERE name = ?`） |
| T-18-02 | Information Disclosure | GetCategoryByName error | accept | 错误消息不暴露敏感信息 |
</threat_model>

<verification>
## Per-Task Verification

| Task | Command |
|------|---------|
| Task 1 | `grep -n "CategoryName" internal/storage/database.go` |
| Task 2 | `grep -n "GetCategoryByName" internal/storage/database.go` |
| Task 3 | `grep -n "category" internal/cli/commands/article.go` |

## End-to-End Verification

```bash
# 编译 CLI
go build -o blogwatcher ./cmd/blogwatcher

# 测试 --category flag 帮助
./blogwatcher article list --help | grep -q "category"

# 测试不存在的分类报错
./blogwatcher article list --category nonexistent 2>&1 | grep -q "不存在"
```
</verification>

<success_criteria>
1. CLI `article list --help` 显示 --category 参数说明
2. CLI `article list --category <name>` 返回该分类下的文章
3. CLI `article list --category nonexistent` 输出 "分类 'nonexistent' 不存在" 并退出
4. CLI `article list --category tech --unread` 组合过滤生效
5. grep 验证所有代码改动存在
</success_criteria>

<output>
After completion, create `.planning/phases/18-category-display-cli/18-01-SUMMARY.md`
</output>