---
phase: 16-database-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [internal/storage/database.go, internal/model/model.go]
autonomous: true
requirements: [CATG-01, CATG-02]
user_setup: []

must_haves:
  truths:
    - "categories 表存在，包含 id, name, created_at 字段"
    - "blogs 表包含 category_id 字段（nullable foreign key）"
    - "现有数据未受影响，迁移向后兼容"
    - "Model 包含 Category struct"
  artifacts:
    - path: "internal/storage/database.go"
      provides: "Schema migration logic"
      contains: "CREATE TABLE IF NOT EXISTS categories"
    - path: "internal/model/model.go"
      provides: "Category model definition"
      contains: "type Category struct"
  key_links:
    - from: "internal/storage/database.go"
      to: "sqlite_master / PRAGMA table_info"
      via: "tableExists / columnExists"
      pattern: "tableExists\\('categories'\\)|columnExists\\('blogs', 'category_id'\\)"
---

<objective>
创建 categories 表并添加 blogs.category_id 字段，支持分类系统基础设施。

Purpose: 为博客分类功能提供数据库基础，允许用户将博客按分类组织。
Output: categories 表、blogs.category_id 字段、Category model struct。
</objective>

<execution_context>
@C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 当前schema
blogs: id, name, url, feed_url, scrape_selector, last_scanned
articles: id, blog_id, title, url, thumbnail_url, published_date, discovered_date, is_read, has_note

# 迁移机制
ensureMigrations() 方法检查列/表是否存在，使用 idempotent pattern
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 categories 表和 Category model</name>
  <files>internal/storage/database.go, internal/model/model.go</files>
  <read_first>
    - internal/storage/database.go (existing migration pattern)
    - internal/model/model.go (existing model definitions)
  </read_first>
  <action>
在 internal/model/model.go 添加 Category struct：
```go
type Category struct {
    ID        int64
    Name      string
    CreatedAt time.Time
}
```

在 internal/storage/database.go 的 ensureMigrations() 方法中添加 categories 表迁移：
- 在方法开始处，使用 tableExists("categories") 检查表是否存在
- 如果不存在，执行：
```sql
CREATE TABLE IF NOT EXISTS categories (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL UNIQUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```
- 添加迁移检查逻辑：`if !db.tableExists("categories") { ... }`

注意：遵循现有迁移 pattern（idempotent，先检查后添加），确保向后兼容。
  </action>
  <verify>
    <automated>go test ./internal/storage -run TestDatabaseMigration</automated>
  </verify>
  <acceptance_criteria>
    - internal/model/model.go 包含 `type Category struct`
    - internal/storage/database.go 包含 `CREATE TABLE IF NOT EXISTS categories`
    - internal/storage/database.go 包含 `if !db.tableExists("categories")`
    - categories 表创建成功，包含 id, name, created_at 字段
    - 迁移向后兼容，现有数据不受影响
  </acceptance_criteria>
  <done>
categories 表创建成功，包含 id (INTEGER PRIMARY KEY), name (TEXT NOT NULL UNIQUE), created_at (TIMESTAMP) 字段；Category struct 定义完成；迁移向后兼容，现有数据不受影响。
  </done>
</task>

<task type="auto">
  <name>Task 2: 添加 blogs.category_id 字段</name>
  <files>internal/storage/database.go, internal/model/model.go</files>
  <read_first>
    - internal/storage/database.go (existing column migration pattern)
    - internal/model/model.go (existing Blog struct)
  </read_first>
  <action>
在 internal/model/model.go 的 Blog struct 添加 CategoryID 字段：
```go
type Blog struct {
    ID             int64
    Name           string
    URL            string
    FeedURL        string
    ScrapeSelector string
    LastScanned    *time.Time
    CategoryID     *int64 // 分类ID（nullable，指向 categories.id）
}
```

在 internal/storage/database.go 的 ensureMigrations() 方法中添加 category_id 列迁移：
- 使用 columnExists("blogs", "category_id") 检查列是否存在
- 如果不存在，执行：
```sql
ALTER TABLE blogs ADD COLUMN category_id INTEGER REFERENCES categories(id)
```
- 添加迁移检查逻辑：`if !db.columnExists("blogs", "category_id") { ... }`

注意：字段为 nullable（允许 blog 不属于任何分类），使用 foreign key constraint REFERENCES categories(id)。
  </action>
  <verify>
    <automated>go test ./internal/storage -run TestDatabaseMigration</automated>
  </verify>
  <acceptance_criteria>
    - internal/model/model.go Blog struct 包含 `CategoryID *int64`
    - internal/storage/database.go 包含 `ALTER TABLE blogs ADD COLUMN category_id INTEGER REFERENCES categories(id)`
    - internal/storage/database.go 包含 `if !db.columnExists("blogs", "category_id")`
    - blogs 表成功添加 category_id 字段（nullable foreign key）
    - 迁移向后兼容，现有 blog 数据不受影响
  </acceptance_criteria>
  <done>
blogs 表添加 category_id INTEGER 字段（nullable foreign key to categories.id）；Blog struct 包含 CategoryID *int64 字段；迁移向后兼容，现有数据不受影响。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database write | Schema migrations modify persistent data store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | ensureMigrations | mitigate | Use IF NOT EXISTS / IF NOT NULL checks before ALTER |
| T-16-02 | Tampering | foreign key | accept | SQLite foreign_keys pragma enabled in DSN |
</threat_model>

<verification>
- categories 表创建成功（id, name, created_at）
- blogs.category_id 字段添加成功（nullable foreign key）
- Category model struct 定义完成
- Blog.CategoryID 字段添加完成
- 迁移向后兼容，现有数据不受影响
</verification>

<success_criteria>
1. categories 表存在，包含 id INTEGER PRIMARY KEY, name TEXT NOT NULL UNIQUE, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
2. blogs 表包含 category_id INTEGER 字段（nullable foreign key to categories.id）
3. Category struct 定义：ID int64, Name string, CreatedAt time.Time
4. Blog struct 包含 CategoryID *int64 字段
5. 数据库迁移向后兼容，现有数据不受影响
6. go test ./internal/storage 通过
</success_criteria>

<output>
After completion, create `.planning/phases/16-database-schema/16-01-SUMMARY.md`
</output>