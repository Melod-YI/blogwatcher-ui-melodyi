---
phase: 13-cli-notes-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified:
  - internal/cli/commands/note.go
  - internal/cli/commands/root.go
autonomous: true
requirements:
  - NOTE-01
  - NOTE-02
  - NOTE-03
  - NOTE-06
user_setup: []

must_haves:
  truths:
    - "用户执行 `blogwatcher note --article-id 42 --file ~/note.md` 成功写入备注"
    - "源文件内容被完整复制（不依赖源文件后续变化）"
    - "用户执行 `blogwatcher note delete --article-id 42` 成功删除备注"
    - "缺少 --article-id 或 --file 时输出错误消息并退出"
    - "备注文件存储于 ~/.blogwatcher/notes/ 目录"
  artifacts:
    - path: "internal/cli/commands/note.go"
      provides: "CLI note and note delete commands"
      contains:
        - "func NewNoteCmd()"
        - "func NewNoteDeleteCmd()"
        - "func runNote()"
        - "func runNoteDelete()"
      min_lines: 150
    - path: "internal/cli/commands/root.go"
      provides: "Command registration"
      contains:
        - "rootCmd.AddCommand(NewNoteCmd())"
      min_lines: 60
    - path: "~/.blogwatcher/notes/{id}.md"
      provides: "Note file storage"
      exists_after: "After running note command"
  key_links:
    - from: "runNote"
      to: "~/.blogwatcher/notes/{id}.md"
      via: "os.ReadFile + os.WriteFile"
      pattern: "os\\.WriteFile.*notes.*\\.md"
    - from: "runNote"
      to: "UpdateArticleHasNote"
      via: "db.UpdateArticleHasNote(id, true)"
      pattern: "UpdateArticleHasNote.*true"
    - from: "runNoteDelete"
      to: "~/.blogwatcher/notes/{id}.md"
      via: "os.Remove"
      pattern: "os\\.Remove.*notes.*\\.md"
    - from: "runNoteDelete"
      to: "UpdateArticleHasNote"
      via: "db.UpdateArticleHasNote(id, false)"
      pattern: "UpdateArticleHasNote.*false"
---

<objective>
实现 CLI note 命令，提供备注写入和删除功能。

Purpose: 用户通过 CLI 写入和删除文章备注，备注以 Markdown 文件存储。
Output: note.go 包含 note 和 note delete 命令，root.go 注册 note 命令。
</objective>

<execution_context>
@C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cli-notes-infrastructure/13-CONTEXT.md
@.planning/phases/13-cli-notes-infrastructure/13-PATTERNS.md
@.planning/phases/13-cli-notes-infrastructure/13-01-SUMMARY.md
@.planning/phases/13-cli-notes-infrastructure/13-02-SUMMARY.md

<interfaces>
<!-- 从 article.go 和 database.go 提取的关键接口 -->

数据库连接模式 (article.go:119-136):
```go
dbPath := flags.DBPath()
if dbPath == "" {
    var err error
    dbPath, err = storage.DefaultDBPath()
    if err != nil {
        fmt.Fprintf(os.Stderr, "获取默认数据库路径失败: %v\n", err)
        os.Exit(1)
    }
}

db, err := storage.OpenDatabase(dbPath)
if err != nil {
    fmt.Fprintf(os.Stderr, "打开数据库失败: %v\n", err)
    os.Exit(1)
}
defer db.Close()
```

命令组注册模式 (root.go:31-36):
```go
rootCmd.AddCommand(NewServeCmd())
rootCmd.AddCommand(NewBlogCmd())
rootCmd.AddCommand(NewArticleCmd())
```

子命令定义模式 (article.go:79-97):
```go
func NewMarkReadCmd() *cobra.Command {
    cmd := &cobra.Command{
        Use:   "mark-read [id]",
        Short: "标记文章已读",
        Long: `标记指定文章已读...`,
        Args: cobra.MaximumNArgs(1),
        Run:  runMarkRead,
    }
    cmd.Flags().Bool("all", false, "标记所有未读文章为已读")
    return cmd
}
```

文件操作模式 (database.go:21-27 + PATTERNS.md):
```go
home, err := os.UserHomeDir()
if err != nil {
    return "", err
}
notesDir := filepath.Join(home, ".blogwatcher", "notes")

// 创建目录（幂等）
if err := os.MkdirAll(notesDir, 0o755); err != nil {
    return fmt.Errorf("创建备注目录失败: %w", err)
}

// 文件路径
notePath := filepath.Join(notesDir, fmt.Sprintf("%d.md", articleID))

// 读取源文件
content, err := os.ReadFile(filePath)
if err != nil {
    return fmt.Errorf("读取文件失败: %w", err)
}

// 写入目标文件（覆盖）
if err := os.WriteFile(notePath, content, 0o644); err != nil {
    return fmt.Errorf("写入备注失败: %w", err)
}

// 删除文件
if err := os.Remove(notePath); err != nil && !os.IsNotExist(err) {
    return fmt.Errorf("删除备注失败: %w", err)
}
```

Plan 13-01 提供的方法：
- `GetArticleByID(id int64) (*model.Article, error)` — 验证文章存在
- `UpdateArticleHasNote(id int64, hasNote bool) error` — 更新备注状态

Plan 13-02 提供的模型更新：
- `Article.HasNote bool` — HasNote 字段
- `scanArticle` 包含 has_note 扫描
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 note.go 文件，定义 note 命令结构</name>
  <files>internal/cli/commands/note.go</files>
  <read_first>
    - internal/cli/commands/article.go (查看命令组结构和 NewCmd 模式)
    - internal/cli/commands/blog.go (查看子命令组织模式)
    - .planning/phases/13-cli-notes-infrastructure/13-CONTEXT.md (确认 D-01, D-02, D-03 决策)
    - .planning/phases/13-cli-notes-infrastructure/13-PATTERNS.md (参考 CLI 命令结构模式)
  </read_first>
  <behavior>
    - Test 1: NewNoteCmd() 返回 *cobra.Command
    - Test 2: note 命令包含 --article-id 和 --file flags
    - Test 3: note 命令包含 delete 子命令
    - Test 4: go build ./internal/cli/commands/ 成功
  </behavior>
  <action>
创建 internal/cli/commands/note.go 文件，定义 note 命令结构和子命令。

**完整文件内容：**

```go
// ABOUTME: note 子命令定义
// ABOUTME: 提供备注写入和删除功能
package commands

import (
	"fmt"
	"os"
	"path/filepath"

	"github.com/esttorhe/blogwatcher-ui/v2/internal/cli/flags"
	"github.com/esttorhe/blogwatcher-ui/v2/internal/storage"
	"github.com/spf13/cobra"
)

// NewNoteCmd 创建 note 命令（命令组）
// note 是一个命令组，包含顶层写入命令和 delete 子命令
func NewNoteCmd() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "note",
		Short: "备注管理命令",
		Long: `备注管理命令，提供备注写入和删除功能。

	子命令：
	  delete    删除文章备注

	示例：
	  blogwatcher note --article-id 1 --file ~/note.md
	  blogwatcher note delete --article-id 1`,
	}

	// 添加顶层命令 flags（用于写入备注）
	cmd.Flags().Int64("article-id", 0, "文章 ID（必填）")
	cmd.Flags().String("file", "", "备注文件路径（必填）")
	cmd.MarkFlagRequired("article-id")
	cmd.MarkFlagRequired("file")

	// 添加子命令
	cmd.AddCommand(NewNoteDeleteCmd())

	// 设置顶层命令的 Run 函数
	cmd.Run = runNote

	return cmd
}

// NewNoteDeleteCmd 创建 delete 子命令
func NewNoteDeleteCmd() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "delete --article-id <id>",
		Short: "删除文章备注",
		Long: `删除指定文章的备注文件并更新数据库状态。

	示例：
	  blogwatcher note delete --article-id 1`,
		Run: runNoteDelete,
	}

	cmd.Flags().Int64("article-id", 0, "文章 ID（必填）")
	cmd.MarkFlagRequired("article-id")

	return cmd
}
```

**文件结构说明：**
- Package: commands（与 article.go 同包）
- Import 包含：fmt, os, filepath, flags, storage, cobra
- NewNoteCmd(): note 命令组，包含 --article-id 和 --file flags
- NewNoteDeleteCmd(): delete 子命令，仅包含 --article-id flag
- 使用 MarkFlagRequired() 标记必填参数（D-15 决策）
</action>
  <verify>
    <automated>go build ./internal/cli/commands/</automated>
    <manual>grep -n "func NewNoteCmd\|func NewNoteDeleteCmd" internal/cli/commands/note.go</manual>
  </verify>
  <acceptance_criteria>
    - note.go 文件存在于 internal/cli/commands/
    - note.go 包含 `func NewNoteCmd() *cobra.Command`
    - note.go 包含 `func NewNoteDeleteCmd() *cobra.Command`
    - NewNoteCmd 包含 `--article-id` 和 `--file` flags（必填）
    - NewNoteDeleteCmd 包含 `--article-id` flag（必填）
    - go build ./internal/cli/commands/ 成功
  </acceptance_criteria>
  <done>
note.go 文件创建完成，命令结构定义完成，编译通过。
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 runNote 函数（写入备注）</name>
  <files>internal/cli/commands/note.go</files>
  <read_first>
    - internal/cli/commands/note.go (查看命令结构，添加 runNote 函数)
    - internal/cli/commands/article.go (查看 runMarkRead 数据库连接模式)
    - internal/storage/database.go (确认 GetArticleByID 和 UpdateArticleHasNote 方法存在)
    - .planning/phases/13-cli-notes-infrastructure/13-CONTEXT.md (确认 D-05, D-06, D-08, D-11, D-13 决策)
    - .planning/phases/13-cli-notes-infrastructure/13-PATTERNS.md (参考文件操作模式)
  </read_first>
  <behavior>
    - Test 1: runNote(--article-id 有效ID, --file 有效路径) 成功写入备注
    - Test 2: runNote(--article-id 无效ID) 输出错误消息并退出
    - Test 3: runNote(--file 不存在路径) 输出错误消息并退出
    - Test 4: 写入后 article.has_note = TRUE
    - Test 5: 备注 文件存储于 ~/.blogwatcher/notes/{id}.md
    - Test 6: go build ./internal/cli/commands/ 成功
  </behavior>
  <action>
在 note.go 中添加 runNote 函数，实现备注写入逻辑。

**具体添加内容（在 NewNoteDeleteCmd 之后）：**

```go
// runNote 执行 note 命令（写入备注）
func runNote(cmd *cobra.Command, args []string) {
	// 获取数据库路径
	dbPath := flags.DBPath()
	if dbPath == "" {
		var err error
		dbPath, err = storage.DefaultDBPath()
		if err != nil {
			fmt.Fprintf(os.Stderr, "获取默认数据库路径失败: %v\n", err)
			os.Exit(1)
		}
	}

	// 打开数据库
	db, err := storage.OpenDatabase(dbPath)
	if err != nil {
		fmt.Fprintf(os.Stderr, "打开数据库失败: %v\n", err)
		os.Exit(1)
	}
	defer db.Close()

	// 解析 flags
	articleID, _ := cmd.Flags().GetInt64("article-id")
	filePath, _ := cmd.Flags().GetString("file")

	// 验证文章 ID 存在（D-05, D-06）
	article, err := db.GetArticleByID(articleID)
	if err != nil {
		fmt.Fprintf(os.Stderr, "验证文章失败: %v\n", err)
		os.Exit(1)
	}
	if article == nil {
		fmt.Fprintf(os.Stderr, "文章 ID %d 不存在\n", articleID)
		os.Exit(1)
	}

	// 获取备注目录路径（D-13）
	home, err := os.UserHomeDir()
	if err != nil {
		fmt.Fprintf(os.Stderr, "获取用户目录失败: %v\n", err)
		os.Exit(1)
	}
	notesDir := filepath.Join(home, ".blogwatcher", "notes")

	// 创建目录（幂等，D-13, D-14）
	if err := os.MkdirAll(notesDir, 0o755); err != nil {
		fmt.Fprintf(os.Stderr, "创建备注目录失败: %v\n", err)
		os.Exit(1)
	}

	// 文件路径（D-06）
	notePath := filepath.Join(notesDir, fmt.Sprintf("%d.md", articleID))

	// 读取源文件（D-08）
	content, err := os.ReadFile(filePath)
	if err != nil {
		fmt.Fprintf(os.Stderr, "读取文件失败: %v\n", err)
		os.Exit(1)
	}

	// 写入目标文件（覆盖，D-11）
	if err := os.WriteFile(notePath, content, 0o644); err != nil {
		fmt.Fprintf(os.Stderr, "写入备注失败: %v\n", err)
		os.Exit(1)
	}

	// 更新数据库状态（D-08, NOTE-08）
	if err := db.UpdateArticleHasNote(articleID, true); err != nil {
		fmt.Fprintf(os.Stderr, "更新备注状态失败: %v\n", err)
		os.Exit(1)
	}

	fmt.Printf("已成功写入文章 %d 的备注\n", articleID)
}
```

**实现要点：**
- 数据库连接：使用 flags.DBPath() + storage.OpenDatabase 模式
- 文章验证：调用 GetArticleByID，若 article == nil 则退出（D-05, D-06）
- 目录创建：使用 os.MkdirAll(notesDir, 0o755)（幂等，D-13, D-14）
- 文件复制：os.ReadFile(filePath) + os.WriteFile(notePath, content, 0o644)（D-08）
- 静默覆盖：直接 WriteFile，无需确认（D-11）
- 数据库更新：UpdateArticleHasNote(id, true)（NOTE-08）
- 错误格式：fmt.Fprintf(os.Stderr, "描述: %v\n", err) + os.Exit(1)
</action>
  <verify>
    <automated>go build ./internal/cli/commands/</automated>
    <manual>grep -n "func runNote" internal/cli/commands/note.go</manual>
  </verify>
  <acceptance_criteria>
    - note.go 包含 `func runNote(cmd *cobra.Command, args []string)`
    - runNote 包含数据库连接逻辑（GetArticleByID 调用）
    - runNote 包含文件读取和写入逻辑（os.ReadFile + os.WriteFile）
    - runNote 包含目录创建逻辑（os.MkdirAll）
    - runNote 包含数据库更新逻辑（UpdateArticleHasNote(id, true)）
    - 错误处理使用 fmt.Fprintf(os.Stderr) + os.Exit(1)
    - go build ./internal/cli/commands/ 成功
  </acceptance_criteria>
  <done>
runNote 函数实现完成，写入备注逻辑完整，编译通过。
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现 runNoteDelete 函数（删除备注）</name>
  <files>internal/cli/commands/note.go</files>
  <read_first>
    - internal/cli/commands/note.go (查看 runNote 函数，添加 runNoteDelete)
    - .planning/phases/13-cli-notes-infrastructure/13-CONTEXT.md (确认 D-17 决策)
    - .planning/phases/13-cli-notes-infrastructure/13-PATTERNS.md (参考删除文件模式)
  </read_first>
  <behavior>
    - Test 1: runNoteDelete(--article-id 有备注的ID) 成功删除备注
    - Test 2: runNoteDelete(--article-id 无备注的ID) 输出错误消息并退出
    - Test 3: 删除后 article.has_note = FALSE
    - Test 4: 备注 文件被删除（~/.blogwatcher/notes/{id}.md 不存在）
    - Test 5: go build ./internal/cli/commands/ 成功
  </behavior>
  <action>
在 note.go 中添加 runNoteDelete 函数，实现备注删除逻辑。

**具体添加内容（在 runNote 之后）：**

```go
// runNoteDelete 执行 note delete 命令（删除备注）
func runNoteDelete(cmd *cobra.Command, args []string) {
	// 获取数据库路径
	dbPath := flags.DBPath()
	if dbPath == "" {
		var err error
		dbPath, err = storage.DefaultDBPath()
		if err != nil {
			fmt.Fprintf(os.Stderr, "获取默认数据库路径失败: %v\n", err)
			os.Exit(1)
		}
	}

	// 打开数据库
	db, err := storage.OpenDatabase(dbPath)
	if err != nil {
		fmt.Fprintf(os.Stderr, "打开数据库失败: %v\n", err)
		os.Exit(1)
	}
	defer db.Close()

	// 解析 flags
	articleID, _ := cmd.Flags().GetInt64("article-id")

	// 验证文章 ID 存在
	article, err := db.GetArticleByID(articleID)
	if err != nil {
		fmt.Fprintf(os.Stderr, "验证文章失败: %v\n", err)
		os.Exit(1)
	}
	if article == nil {
		fmt.Fprintf(os.Stderr, "文章 ID %d 不存在\n", articleID)
		os.Exit(1)
	}

	// 获取备注目录路径
	home, err := os.UserHomeDir()
	if err != nil {
		fmt.Fprintf(os.Stderr, "获取用户目录失败: %v\n", err)
		os.Exit(1)
	}
	notesDir := filepath.Join(home, ".blogwatcher", "notes")
	notePath := filepath.Join(notesDir, fmt.Sprintf("%d.md", articleID))

	// 删除备注文件（D-17）
	err = os.Remove(notePath)
	if err != nil {
		if os.IsNotExist(err) {
			fmt.Fprintf(os.Stderr, "文章 %d 没有备注\n", articleID)
			os.Exit(1)
		}
		fmt.Fprintf(os.Stderr, "删除备注失败: %v\n", err)
		os.Exit(1)
	}

	// 更新数据库状态（NOTE-08）
	if err := db.UpdateArticleHasNote(articleID, false); err != nil {
		fmt.Fprintf(os.Stderr, "更新备注状态失败: %v\n", err)
		os.Exit(1)
	}

	fmt.Printf("已成功删除文章 %d 的备注\n", articleID)
}
```

**实现要点：**
- 数据库连接：与 runNote 相同的模式
- 文章验证：GetArticleByID，确保文章存在
- 文件删除：os.Remove(notePath)
- 不存在处理：os.IsNotExist(err) 时输出错误并退出（D-17）
- 数据库更新：UpdateArticleHasNote(id, false)（NOTE-08）
- 错误消息："文章 %d 没有备注"（D-17）
</action>
  <verify>
    <automated>go build ./internal/cli/commands/</automated>
    <manual>grep -n "func runNoteDelete" internal/cli/commands/note.go</manual>
  </verify>
  <acceptance_criteria>
    - note.go 包含 `func runNoteDelete(cmd *cobra.Command, args []string)`
    - runNoteDelete 包含数据库连接逻辑
    - runNoteDelete 包含文件删除逻辑（os.Remove）
    - runNoteDelete 包含 os.IsNotExist 检查（D-17）
    - runNoteDelete 包含 UpdateArticleHasNote(id, false)
    - 错误消息包含 "没有备注"
    - go build ./internal/cli/commands/ 成功
  </acceptance_criteria>
  <done>
runNoteDelete 函数实现完成，删除备注逻辑完整，编译通过。
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 注册 note 命令到 root.go</name>
  <files>internal/cli/commands/root.go</files>
  <read_first>
    - internal/cli/commands/root.go (查看 init() 函数和 AddCommand 模式)
    - internal/cli/commands/note.go (确认 NewNoteCmd 函数存在)
  </read_first>
  <behavior>
    - Test 1: root.go init() 包含 rootCmd.AddCommand(NewNoteCmd())
    - Test 2: go build ./cmd/blogwatcher 成功
    - Test 3: blogwatcher --help 显示 note 命令
    - Test 4: blogwatcher note --help 显示帮助信息
  </behavior>
  <action>
在 root.go 的 init() 函数中注册 note 命令。

**具体修改（root.go init() 函数）：**

在 `rootCmd.AddCommand(NewArticleCmd())` 之后添加：

```go
// 添加 note 子命令
rootCmd.AddCommand(NewNoteCmd())
```

**完整 init() 函数（修改后）：**
```go
func init() {
	// 设置版本输出模板
	rootCmd.SetVersionTemplate(`{{.Name}} {{.Version}}
`)
	// 添加 serve 子命令
	rootCmd.AddCommand(NewServeCmd())
	// 添加 blog 子命令
	rootCmd.AddCommand(NewBlogCmd())
	// 添加 article 子命令
	rootCmd.AddCommand(NewArticleCmd())
	// 添加 note 子命令
	rootCmd.AddCommand(NewNoteCmd())
}
```

**位置说明：**
- 在 root.go 第 36 行（NewArticleCmd() 之后）添加
- 保持命令注册顺序：serve → blog → article → note
</action>
  <verify>
    <automated>go build ./cmd/blogwatcher</automated>
    <manual>grep -n "AddCommand(NewNoteCmd())" internal/cli/commands/root.go</manual>
  </verify>
  <acceptance_criteria>
    - root.go init() 包含 `rootCmd.AddCommand(NewNoteCmd())`
    - 命令注册顺序：serve, blog, article, note
    - go build ./cmd/blogwatcher 成功
    - 命令注册位于 NewArticleCmd() 之后
  </acceptance_criteria>
  <done>
note 命令注册完成，CLI 编译通过，命令可用。
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → File System | 用户输入的文件路径直接用于文件操作 |
| CLI → Database | 用户输入的文章 ID 直接用于数据库查询和更新 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Spoofing | note command | accept | CLI 本地工具，无身份验证需求 |
| T-13-07 | Tampering | runNote file path | mitigate | 使用 os.ReadFile 验证源文件存在 |
| T-13-08 | Tampering | runNoteDelete | mitigate | os.IsNotExist 检查防止误删 |
| T-13-09 | Injection | GetArticleByID | accept | 使用参数化查询，SQL 注入风险低 |
| T-13-10 | Information Disclosure | note file | accept | 备注内容为用户自有数据 |
</threat_model>

<verification>
## 整体阶段验证

1. **编译验证：**
   ```bash
   go build ./internal/cli/commands/
   go build ./cmd/blogwatcher
   ```

2. **命令存在验证：**
   ```bash
   ./blogwatcher --help | grep note
   ./blogwatcher note --help
   ./blogwatcher note delete --help
   ```

3. **功能验证（需要有效文章 ID）：**
   ```bash
   # 创建测试备注文件
   echo "# Test Note" > /tmp/test-note.md

   # 写入备注（需要真实文章 ID）
   ./blogwatcher note --article-id 1 --file /tmp/test-note.md

   # 验证备注文件存在
   ls ~/.blogwatcher/notes/1.md

   # 删除备注
   ./blogwatcher note delete --article-id 1

   # 验证备注文件已删除
   ls ~/.blogwatcher/notes/1.md
   ```

4. **参数验证：**
   ```bash
   # 缺少 --file 应报错
   ./blogwatcher note --article-id 1
   # 应输出："required flag(s) "file" not set"

   # 缺少 --article-id 应报错
   ./blogwatcher note delete
   # 应输出："required flag(s) "article-id" not set"
   ```
</verification>

<success_criteria>
1. note 命令注册到 root.go
2. note 命令支持 --article-id 和 --file 参数（必填）
3. note delete 命令支持 --article-id 参数（必填）
4. 写入备注成功后文件存在于 ~/.blogwatcher/notes/{id}.md
5. 删除备注成功后文件不存在
6. 写入备注后 article.has_note = TRUE
7. 删除备注后 article.has_note = FALSE
8. 缺少必填参数时报错退出
9. 所有代码编译通过
</success_criteria>

<output>
完成后创建 `.planning/phases/13-cli-notes-infrastructure/13-03-SUMMARY.md`
</output>