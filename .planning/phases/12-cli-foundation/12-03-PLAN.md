---
phase: 12-cli-foundation
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - internal/storage/database.go
  - internal/cli/commands/article.go
  - internal/cli/output/table.go
  - internal/cli/output/json.go
  - internal/cli/output/simple.go
autonomous: true
requirements:
  - CLI-02
  - CLI-03
  - CLI-04
user_setup: []

must_haves:
  truths:
    - "运行 blogwatcher article list 显示所有文章（默认表格格式）"
    - "运行 blogwatcher article list --blog <name> 按博客筛选"
    - "运行 blogwatcher article list --unread/--read 按状态筛选"
    - "运行 blogwatcher article list --after <date> 按日期筛选"
    - "运行 blogwatcher article list --format json 输出 JSON 格式"
    - "运行 blogwatcher article mark-read <id> 标记单篇已读"
    - "运行 blogwatcher article mark-read --all 标记全部已读"
    - "运行 blogwatcher article mark-unread <id> 标记单篇未读"
    - "筛选参数可组合使用（--blog + --unread + --after）"
  artifacts:
    - path: "internal/cli/commands/article.go"
      provides: "article 子命令（list + mark-read + mark-unread）"
      min_lines: 200
      contains: "func NewArticleCmd"
    - path: "internal/cli/output/table.go"
      provides: "表格格式输出"
      min_lines: 40
      contains: "func FormatTable"
    - path: "internal/cli/output/json.go"
      provides: "JSON 格式输出"
      min_lines: 30
      contains: "func FormatJSON"
    - path: "internal/cli/output/simple.go"
      provides: "简洁格式输出"
      min_lines: 30
      contains: "func FormatSimple"
    - path: "internal/storage/database.go"
      provides: "日期筛选方法"
      min_lines: 30
      contains: "func.*ListArticlesWithFilters"
  key_links:
    - from: "internal/cli/commands/article.go"
      to: "internal/cli/flags.go"
      via: "dbPath variable"
      pattern: "flags\\.dbPath"
    - from: "internal/cli/commands/article.go"
      to: "internal/storage"
      via: "storage.OpenDatabase / ListArticlesWithFilters"
      pattern: "storage\\.OpenDatabase"
    - from: "internal/cli/commands/article.go"
      to: "internal/cli/output"
      via: "FormatTable / FormatJSON / FormatSimple"
      pattern: "output\\.Format"
    - from: "internal/storage/database.go"
      to: "internal/model"
      via: "ArticleWithBlog struct"
      pattern: "model\\.ArticleWithBlog"
---

<objective>
实现 article 子命令：列出文章（多筛选参数和多格式输出）、标记已读/未读。

Purpose: 提供命令行方式查看和管理文章，支持灵活的筛选和格式化输出。
Output: article list、article mark-read、article mark-unread 命令，以及输出格式化器。
</objective>

<execution_context>
@C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-cli-foundation/12-01-SUMMARY.md
@.planning/phases/12-cli-foundation/12-RESEARCH.md

<interfaces>
<!-- 关键接口，供执行者直接使用 -->

From internal/cli/flags.go (Plan 12-01):
```go
var dbPath string  // 全局数据库路径 flag
```

From internal/storage/database.go (现有方法):
```go
func (db *Database) ListBlogs() ([]model.Blog, error)
func (db *Database) GetBlogByName(name string) (*model.Blog, error)
func (db *Database) ListArticlesWithBlog(isRead bool, blogID *int64) ([]model.ArticleWithBlog, error)
func (db *Database) MarkArticleRead(id int64) (bool, error)
func (db *Database) MarkArticleUnread(id int64) (bool, error)
func (db *Database) MarkAllUnreadArticlesRead(blogID *int64) error
```

From internal/model/model.go:
```go
type ArticleWithBlog struct {
    ID             int64
    BlogID         int64
    Title          string
    URL            string
    ThumbnailURL   string
    PublishedDate  *time.Time
    DiscoveredDate *time.Time
    IsRead         bool
    BlogName       string
    BlogURL        string
}
```

Cobra 命令结构:
```go
var articleCmd = &cobra.Command{
    Use:   "article",
    Short: "文章管理命令",
}

var listCmd = &cobra.Command{
    Use:   "list",
    Short: "列出文章",
    Run:   runList,
}

var markReadCmd = &cobra.Command{
    Use:   "mark-read [id]",
    Short: "标记文章已读",
    Args:  cobra.MaximumNArgs(1),
    Run:   runMarkRead,
}
```

输出格式定义:
- table: "| ID | Title | Blog | Status | Time |"
- json: JSON 数组 `[{"id":1,"title":"...","blog":"...","read":false,...}]`
- simple: "[未读] Article Title (Blog Name)"

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 storage 方法支持日期筛选</name>
  <files>
    internal/storage/database.go
  </files>
  <action>
    1. 在 internal/storage/database.go 中添加新方法：
       - ListArticlesWithFilters(opts ListFilterOptions) ([]model.ArticleWithBlog, error)
    2. 定义 ListFilterOptions 结构：
       - BlogName string（空表示所有博客）
       - IsRead *bool（nil 表示所有状态）
       - AfterDate *time.Time（nil 表示无日期限制）
       - Limit int（0 表示无限制）
    3. 实现 ListArticlesWithFilters：
       - 构建 SQL 查询：SELECT a.*, b.name, b.url FROM articles a INNER JOIN blogs b
       - 添加 WHERE 条件：
         * BlogName 不为空：通过子查询获取 blog_id（SELECT id FROM blogs WHERE name = ?）
         * IsRead 不为 nil：AND is_read = ?
         * AfterDate 不为 nil：AND published_date >= ?（或 discovered_date >= ?）
       - 添加 ORDER BY：ORDER BY published_date DESC, discovered_date DESC
       - 添加 LIMIT：如果 opts.Limit > 0
       - 使用 db.conn.Query 执行查询
       - 扫描结果到 []model.ArticleWithBlog
    4. 使用中文注释
    5. 确保方法在现有方法之后添加（文件末尾附近）
  </action>
  <verify>
    <automated>
      cd "C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui" && grep -q "ListArticlesWithFilters" internal/storage/database.go && echo "Method added"
    </automated>
  </verify>
  <done>
    - ListFilterOptions 结构定义
    - ListArticlesWithFilters 方法实现
    - 支持 BlogName、IsRead、AfterDate、Limit 筛选
    - 编译成功（方法正确实现）
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建输出格式化器</name>
  <files>
    internal/cli/output/table.go
    internal/cli/output/json.go
    internal/cli/output/simple.go
  </files>
  <action>
    1. 创建 internal/cli/output 目录
    2. 创建 internal/cli/output/table.go：
       - FormatTable(articles []model.ArticleWithBlog) string 函数
       - 使用 text/tabwriter 或手动格式化表格
       - 表头："ID", "Title", "Blog", "Status", "Published"
       - 每行输出：ID（数字）、Title（截断到 50 字符）、BlogName、Status（"已读"/"未读"）、Time（相对时间或日期）
       - 使用 fmt.Sprintf 和字符串拼接
       - 返回格式化的表格字符串
    3. 创建 internal/cli/output/json.go：
       - FormatJSON(articles []model.ArticleWithBlog) string 函数
       - 使用 encoding/json.Marshal
       - 定义输出结构（简化版）：{"id", "title", "url", "blog", "read", "published"}
       - 返回 JSON 字符串（格式化或紧凑）
    4. 创建 internal/cli/output/simple.go：
       - FormatSimple(articles []model.ArticleWithBlog) string 函数
       - 格式："[状态] Title (BlogName) - PublishedDate"
       - 状态："[已读]" 或 "[未读]"
       - 每行一个文章
       - 返回字符串（多行）
    5. 使用中文注释
    6. 确保每个文件都有 ABOUTME 注释描述用途
  </action>
  <verify>
    <automated>
      cd "C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui" && go build ./internal/cli/output && echo "Output package builds"
    </automated>
  </verify>
  <done>
    - internal/cli/output 目录存在
    - table.go 存在，FormatTable 函数实现
    - json.go 存在，FormatJSON 函数实现
    - simple.go 存在，FormatSimple 函数实现
    - 编译成功
  </done>
</task>

<task type="auto">
  <name>Task 3: 实现 article 子命令框架</name>
  <files>
    internal/cli/commands/article.go
  </files>
  <action>
    1. 创建 internal/cli/commands/article.go
    2. 定义 NewArticleCmd() 函数返回 *cobra.Command：
       - Use: "article"
       - Short: "文章管理命令"
       - 不定义 Run（article 是命令组）
    3. 定义 NewListCmd() 函数：
       - Use: "list"
       - Short: "列出文章"
       - Long: "列出文章，支持筛选参数和多种输出格式。"
       - Run: runList
       - 添加 flags：
         * --blog string（博客名称筛选）
         * --unread（仅未读文章）
         * --read（仅已读文章）
         * --after string（日期筛选，格式 YYYY-MM-DD）
         * --format string（输出格式：table|json|simple，默认 table）
       - 标记 --unread 和 --read 为互斥（使用 cmd.MarkFlagMutuallyExclusive）
    4. 定义 NewMarkReadCmd() 函数：
       - Use: "mark-read [id]"
       - Short: "标记文章已读"
       - Long: "标记指定文章已读，或使用 --all 标记所有未读文章。"
       - Args: cobra.MaximumNArgs(1)
       - Run: runMarkRead
       - 添加 flag：--all（标记所有未读文章）
    5. 定义 NewMarkUnreadCmd() 函数：
       - Use: "mark-unread <id>"
       - Short: "标记文章未读"
       - Args: cobra.ExactArgs(1)（必须提供 id）
       - Run: runMarkUnread
    6. 在 NewArticleCmd 中添加子命令：
       - articleCmd.AddCommand(NewListCmd())
       - articleCmd.AddCommand(NewMarkReadCmd())
       - articleCmd.AddCommand(NewMarkUnreadCmd())
    7. 在 root.go 中添加 article 子命令到 rootCmd
    8. 使用中文注释
  </action>
  <verify>
    <automated>
      cd "C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui" && go build ./internal/cli/commands && echo "Commands build"
    </automated>
  </verify>
  <done>
    - internal/cli/commands/article.go 存在
    - NewArticleCmd()、NewListCmd()、NewMarkReadCmd()、NewMarkUnreadCmd() 函数定义
    - list、mark-read、mark-unread 子命令添加到 article 命令
    - article 命令添加到 rootCmd
    - list 命令包含 --blog、--unread、--read、--after、--format flags
    - mark-read 命令包含 --all flag
    - 编译成功
  </done>
</task>

<task type="auto">
  <name>Task 4: 实现 list 命令执行逻辑</name>
  <files>
    internal/cli/commands/article.go
  </files>
  <action>
    1. 实现 runList(cmd, args) 函数：
       - 获取数据库路径：使用 flags.dbPath 或 storage.DefaultDBPath()
       - 打开数据库：storage.OpenDatabase(dbPath)，defer db.Close()
       - 解析 flags：
         * blogName := cmd.Flag("blog").Value.String()
         * unread := cmd.Flag("unread").Changed
         * read := cmd.Flag("read").Changed
         * afterStr := cmd.Flag("after").Value.String()
         * format := cmd.Flag("format").Value.String()
       - 构建筛选选项：
         * BlogName：如果 blogName 不为空
         * IsRead：
           - unread 为 true：IsRead = false
           - read 为 true：IsRead = true
           - 都为 false：IsRead = nil（所有状态）
         * AfterDate：解析 afterStr（YYYY-MM-DD 格式）为 time.Time
       - 调用 db.ListArticlesWithFilters(opts)
       - 根据格式调用输出格式化器：
         * format == "table"：output.FormatTable(articles)
         * format == "json"：output.FormatJSON(articles)
         * format == "simple"：output.FormatSimple(articles)
         * 默认：output.FormatTable(articles)
       - 输出到 stdout：fmt.Println(result)
       - 如果没有文章：输出"没有找到文章"
    2. 添加错误处理：
       - 日期解析失败：返回 fmt.Errorf("日期格式错误: %w", err)
       - 数据库查询失败：输出错误到 stderr
       - 博客名称不存在：输出"博客 '[name]' 不存在"
    3. 使用中文注释
    4. 确保 runList 在文件末尾（私有函数）
  </action>
  <verify>
    <automated>
      cd "C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui" && go build -o blogwatcher.exe ./cmd/blogwatcher && ./blogwatcher.exe article list --help | grep -q "format" && echo "List help works"
    </automated>
  </verify>
  <done>
    - runList 函数实现完整
    - 数据库路径获取正确
    - 筛选参数解析正确（--blog、--unread/--read、--after）
    - 输出格式选择正确（--format table|json|simple）
    - 编译成功
    - ./blogwatcher.exe article list --help 显示帮助
  </done>
</task>

<task type="auto">
  <name>Task 5: 实现 mark-read 和 mark-unread 命令</name>
  <files>
    internal/cli/commands/article.go
  </files>
  <action>
    1. 实现 runMarkRead(cmd, args) 函数：
       - 获取数据库路径：使用 flags.dbPath 或 storage.DefaultDBPath()
       - 打开数据库：storage.OpenDatabase(dbPath)，defer db.Close()
       - 判断 --all flag：
         * 如果 cmd.Flag("all").Changed 为 true：
           - 调用 db.MarkAllUnreadArticlesRead(nil)（标记所有未读）
           - 输出："已标记所有未读文章为已读"
         * 如果 args 长度为 1：
           - 解析文章 ID：strconv.ParseInt(args[0], 10, 64)
           - 调用 db.MarkArticleRead(id)
           - 如果返回 false（文章不存在或已读）：输出"文章不存在或已标记为已读"
           - 如果返回 true：输出"已标记文章 [id] 为已读"
         * 否则（无 --all 且无 id）：
           - 输出错误："需要提供文章 ID 或使用 --all"
    2. 实现 runMarkUnread(cmd, args) 函数：
       - 获取数据库路径：使用 flags.dbPath 或 storage.DefaultDBPath()
       - 打开数据库：storage.OpenDatabase(dbPath)，defer db.Close()
       - 解析文章 ID：strconv.ParseInt(args[0], 10, 64)
       - 调用 db.MarkArticleUnread(id)
       - 如果返回 false：输出"文章不存在或已标记为未读"
       - 如果返回 true：输出"已标记文章 [id] 为未读"
    3. 添加错误处理：
       - ID 解析失败：返回 fmt.Errorf("文章 ID 格式错误: %w", err)
       - 数据库操作失败：输出错误到 stderr
    4. 使用中文注释
    5. 确保 runMarkRead 和 runMarkUnread 在文件末尾
  </action>
  <verify>
    <automated>
      cd "C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui" && go build -o blogwatcher.exe ./cmd/blogwatcher && ./blogwatcher.exe article mark-read --help | grep -q "all" && ./blogwatcher.exe article mark-unread --help && echo "Mark commands help works"
    </automated>
  </verify>
  <done>
    - runMarkRead 函数实现完整
    - runMarkUnread 函数实现完整
    - mark-read 支持单篇文章（参数 id）和批量（--all）
    - mark-unread 支持单篇文章（参数 id）
    - 编译成功
    - ./blogwatcher.exe article mark-read --help 显示帮助
    - ./blogwatcher.exe article mark-unread --help 显示帮助
  </done>
</task>

<task type="auto">
  <name>Task 6: 验证 article 命令功能</name>
  <files>
    (无新文件，验证现有功能)
  </files>
  <action>
    1. 构建 CLI：go build -o blogwatcher.exe ./cmd/blogwatcher
    2. 测试帮助命令：
       - ./blogwatcher.exe article --help
       - ./blogwatcher.exe article list --help
       - ./blogwatcher.exe article mark-read --help
       - ./blogwatcher.exe article mark-unread --help
    3. 测试 list 命令（需要测试数据库）：
       - ./blogwatcher.exe article list（默认表格格式）
       - ./blogwatcher.exe article list --format json（JSON 格式）
       - ./blogwatcher.exe article list --format simple（简洁格式）
       - ./blogwatcher.exe article list --unread（仅未读）
       - ./blogwatcher.exe article list --read（仅已读）
       - ./blogwatcher.exe article list --blog BlogName（按博客筛选）
       - ./blogwatcher.exe article list --after 2026-01-01（日期筛选）
       - ./blogwatcher.exe article list --unread --blog BlogName --after 2026-01-01（组合筛选）
    4. 测试 mark 命令：
       - ./blogwatcher.exe article mark-read 1（标记单篇）
       - ./blogwatcher.exe article mark-read --all（标记全部）
       - ./blogwatcher.exe article mark-unread 1（标记未读）
    5. 测试全局 --db flag：
       - ./blogwatcher.exe --db /path.db article list
    6. 验证输出格式：
       - table：表格对齐，包含 ID、Title、Blog、Status、Published 列
       - json：有效的 JSON 数组
       - simple：每行一个文章，格式 [状态] Title (Blog)
    7. 使用中文注释和日志
  </action>
  <verify>
    <automated>
      cd "C:/workspace/blogwatcher-ui-melodyi/blogwatcher-ui" && go build -o blogwatcher.exe ./cmd/blogwatcher && ./blogwatcher.exe article list --help | grep -q "format" && ./blogwatcher.exe article mark-read --help | grep -q "all" && echo "Article commands verified"
    </automated>
  </verify>
  <done>
    - ./blogwatcher.exe article --help 显示 article 命令组帮助
    - ./blogwatcher.exe article list --help 显示 list 命令帮助（包含所有 flags）
    - ./blogwatcher.exe article mark-read --help 显示 mark-read 命令帮助（包含 --all flag）
    - ./blogwatcher.exe article mark-unread --help 显示 mark-unread 命令帮助
    - ./blogwatcher.exe article list 成功列出文章（默认表格格式）
    - ./blogwatcher.exe article list --format json 输出 JSON 格式
    - ./blogwatcher.exe article list --format simple 输出简洁格式
    - ./blogwatcher.exe article list --unread 仅列出未读文章
    - ./blogwatcher.exe article list --blog BlogName 仅列出指定博客的文章
    - ./blogwatcher.exe article list --after 2026-01-01 仅列出指定日期后的文章
    - ./blogwatcher.exe article mark-read <id> 成功标记单篇文章已读
    - ./blogwatcher.exe article mark-read --all 成功标记所有未读文章已读
    - ./blogwatcher.exe article mark-unread <id> 成功标记单篇文章未读
    - 全局 --db flag 正确传递到所有 article 子命令
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Database | 本地数据库读写 |
| CLI → User Input | 命令参数输入（id、blog name、date） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering | mark-read id | mitigate | 验证 ID 有效性（strconv.ParseInt 错误处理） |
| T-12-08 | Tampering | --after date | mitigate | 验证日期格式（time.Parse 错误处理） |
| T-12-09 | Information Disclosure | article list output | accept | 输出文章标题和博客名，无敏感信息 |
| T-12-10 | Elevation of Privilege | mark-read --all | accept | 本地操作，用户主动执行，无特权 |

</threat_model>

<verification>
## 功能验证

构建测试：
```bash
go build -o blogwatcher.exe ./cmd/blogwatcher
```

命令帮助：
```bash
./blogwatcher.exe article --help
./blogwatcher.exe article list --help
./blogwatcher.exe article mark-read --help
./blogwatcher.exe article mark-unread --help
```

List 命令测试（需要数据库）：
```bash
./blogwatcher.exe article list                     # 默认表格
./blogwatcher.exe article list --format json       # JSON
./blogwatcher.exe article list --format simple     # 简洁
./blogwatcher.exe article list --unread            # 未读
./blogwatcher.exe article list --read              # 已读
./blogwatcher.exe article list --blog BlogName     # 指定博客
./blogwatcher.exe article list --after 2026-01-01  # 日期筛选
./blogwatcher.exe article list --unread --blog BlogName --after 2026-01-01  # 组合
```

Mark 命令测试（需要数据库）：
```bash
./blogwatcher.exe article mark-read 1      # 单篇
./blogwatcher.exe article mark-read --all  # 全部
./blogwatcher.exe article mark-unread 1    # 标记未读
```

全局 flag 测试：
```bash
./blogwatcher.exe --db /path.db article list
```

输出格式验证：
- table：表格对齐，列标题清晰
- json：有效 JSON，包含 id、title、blog、read、published 字段
- simple：每行一个文章，格式 "[已读/未读] Title (Blog)"
</verification>

<success_criteria>
1. ./blogwatcher.exe article --help 显示 article 命令组帮助
2. ./blogwatcher.exe article list --help 显示所有筛选参数和格式选项
3. ./blogwatcher.exe article list 成功列出文章（默认表格格式）
4. ./blogwatcher.exe article list --format json 输出有效 JSON 数组
5. ./blogwatcher.exe article list --format simple 输出简洁列表格式
6. ./blogwatcher.exe article list --unread 仅列出未读文章
7. ./blogwatcher.exe article list --read 仅列出已读文章
8. ./blogwatcher.exe article list --blog <name> 仅列出指定博客的文章
9. ./blogwatcher.exe article list --after <date> 仅列出指定日期后的文章
10. ./blogwatcher.exe article mark-read <id> 成功标记单篇文章已读
11. ./blogwatcher.exe article mark-read --all 成功标记所有未读文章已读
12. ./blogwatcher.exe article mark-unread <id> 成功标记单篇文章未读
13. 筛选参数可组合使用（--blog + --unread + --after）
14. 全局 --db flag 正确传递到所有 article 子命令
15. 错误处理正确（无效 ID、无效日期、博客不存在）
</success_criteria>

<output>
完成后创建 .planning/phases/12-cli-foundation/12-03-SUMMARY.md
</output>